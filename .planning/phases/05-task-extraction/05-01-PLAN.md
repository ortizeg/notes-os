---
phase: 05-task-extraction
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/notes_os/sorter/extractor.py
  - src/notes_os/exceptions.py
  - pyproject.toml
  - tests/sorter/test_extractor.py
autonomous: true
requirements: [TASK-01]
must_haves:
  truths:
    - "extract_tasks(text) returns at least one ExtractedTask when text contains an action phrase, named commitment, or inline date"
    - "extract_tasks(text) returns an empty list for text with no known patterns"
    - "extract_tasks is pure: same input always yields the same output, with no filesystem, config, or network access"
    - "Each of the three LOCKED signal families (action phrases / named commitments / inline dates) is detected by at least one regex"
  artifacts:
    - path: "src/notes_os/sorter/extractor.py"
      provides: "extract_tasks(text: str) -> list[ExtractedTask] pure heuristic scanner + frozen ExtractedTask model"
      contains: "def extract_tasks"
      min_lines: 90
    - path: "tests/sorter/test_extractor.py"
      provides: "Unit tests for every signal family + empty/no-match + purity/determinism"
      contains: "def test_"
      min_lines: 80
  key_links:
    - from: "src/notes_os/sorter/extractor.py"
      to: "pyproject.toml [[tool.mypy.overrides]] module list"
      via: "ExtractedTask is a frozen Pydantic BaseModel — module MUST be in the override list"
      pattern: "notes_os.sorter.extractor"
---

<objective>
Build `extractor.py`: a PURE, deterministic, fully unit-testable heuristic scanner
that finds action items in a note's plain text. No I/O, no config dependency, no
LLM, no new dependencies — stdlib + regex only. This is the core of TASK-01.

Purpose: Phase 5's value rests on reliable, testable extraction. Keeping it pure
(a single `extract_tasks(text: str)` function) means the UI, writer, and controller
integration in plan 05-02 can be built and tested against a stable contract.

Output:
- `src/notes_os/sorter/extractor.py` with the frozen `ExtractedTask` model and
  `extract_tasks(text: str) -> list[ExtractedTask]`.
- `pyproject.toml` mypy override updated (new BaseModel module).
- `tests/sorter/test_extractor.py` with ≥90% coverage of the extractor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@src/notes_os/sorter/models.py
@src/notes_os/exceptions.py
@src/notes_os/config.py
</context>

<environment_notes>
The `pixi` shell function is BROKEN. Use the env binaries directly:
- Type-check:  `.pixi/envs/default/bin/mypy src`
- Lint:        `.pixi/envs/default/bin/ruff check src tests`
- Format:      `.pixi/envs/default/bin/ruff format src tests`
- Tests:       `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80`
- Focused:     `.pixi/envs/default/bin/pytest tests/sorter/test_extractor.py --cov=notes_os.sorter.extractor --cov-report=term-missing`

pre-commit is NOT installed. Before EVERY commit, MANUALLY run in this order:
`ruff format src tests` → `ruff check src tests` → `mypy src`. Run `ruff format` on
the NEW test file especially. Watch RUF001/RUF002/RUF003 for Unicode in test data —
`tests/` already has a per-file-ignore for those; keep test strings ASCII where
possible, otherwise rely on that existing ignore (do NOT add new noqa directives).

After running pixi/mypy/pytest, discard lockfile churn: `git checkout -- pixi.lock`.

Project standards (CLAUDE.md): module docstring FIRST, blank line, then
`from __future__ import annotations`, then imports. mypy strict (zero errors, no
bare `type: ignore`). Ruff full ruleset. Google-style docstrings with full type
annotations. `logging` not `print`. `pathlib` not `os.path`. Frozen Pydantic V2
models. Custom exceptions extend `NotesOSError`.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for extract_tasks across all three signal families</name>
  <files>tests/sorter/test_extractor.py</files>
  <behavior>
    - Action phrases: text "I need to follow up with design" → at least one ExtractedTask;
      each of "need to", "follow up", "TODO", "schedule", "remind", "I will", "we should"
      individually triggers a hit (one parametrized case per phrase).
    - Named commitments: "Sarah will send the deck" → a hit ([Capitalized name] + "will");
      "I promised to review" → a hit.
    - Inline dates/deadlines: "submit by Friday" → a hit; "let's meet next week" → a hit;
      a numeric date like "due 2026-06-30" → a hit.
    - No-match: "The cat sat on the mat." → returns [] (empty list).
    - Empty / whitespace-only input → returns [].
    - Determinism/purity: calling extract_tasks(text) twice on the same input returns
      equal results; the returned objects expose a `.text` str field.
    - Multiple signals in one paragraph yield multiple distinct tasks (no duplicate text).
  </behavior>
  <action>
    Create tests/sorter/test_extractor.py. RED phase — import `extract_tasks` and
    `ExtractedTask` from `notes_os.sorter.extractor` (does not exist yet, so tests fail
    on import). Use `pytest.mark.parametrize` for the seven action phrases and for the
    date forms. Assert `len(result) >= 1` and `result[0].text` is a non-empty str for
    positive cases; assert `result == []` for the negative/empty cases. Add a purity test
    asserting `extract_tasks(t) == extract_tasks(t)` for a fixed string. Do NOT use the
    real `~/.notes-os` dir — this module is pure and touches no filesystem, so no tmp_path
    is needed here. Keep test sentences ASCII to avoid RUF001/002/003. Run the suite to
    confirm it fails on the missing import (RED). Commit: `test(05-01): failing tests for extract_tasks signal families`.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/sorter/test_extractor.py 2>&1 | grep -qE "ModuleNotFoundError|ImportError|errors during collection" && echo RED_OK</automated>
  </verify>
  <done>test_extractor.py exists with parametrized cases for all three signal families plus empty/no-match/purity, and the suite fails because extractor.py does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement extractor.py (ExtractedTask + extract_tasks) to pass the tests</name>
  <files>src/notes_os/sorter/extractor.py, src/notes_os/exceptions.py</files>
  <behavior>
    - All tests from Task 1 pass (GREEN).
    - ExtractedTask is a frozen Pydantic V2 BaseModel with a single `text: str` field.
    - extract_tasks scans line/sentence fragments and returns one ExtractedTask per
      matched fragment, de-duplicated by `.text`, preserving first-seen order
      (deterministic).
  </behavior>
  <action>
    GREEN phase. Create src/notes_os/sorter/extractor.py with module docstring FIRST,
    blank line, then `from __future__ import annotations`, then stdlib `re` and pydantic
    imports. Define frozen `ExtractedTask(BaseModel)` with `model_config = ConfigDict(frozen=True)`
    and field `text: str` (mirror the model style in models.py / session.py). Define
    module-level compiled regexes (`re.compile(..., re.IGNORECASE)`) for the three LOCKED
    signal families EXACTLY as specified in the PRD:
      - Action phrases: alternation over "need to", "follow up", "TODO", "schedule",
        "remind", "I will", "we should".
      - Named commitments: a capitalized-name-followed-by-"will" pattern
        (e.g. `\b[A-Z][a-z]+\s+will\b`) AND a literal "I promised".
      - Inline dates: "by <Weekday>", "next week", and a numeric date form
        (e.g. ISO `\d{4}-\d{2}-\d{2}` and/or `\b\d{1,2}/\d{1,2}\b`).
    Implement `extract_tasks(text: str) -> list[ExtractedTask]`: split *text* into
    candidate fragments (by sentence/newline — use a simple split on `[.\n!?]`), strip
    whitespace, and for each fragment that matches ANY signal regex, emit one
    `ExtractedTask(text=<cleaned fragment>)`. De-duplicate by `.text` preserving order;
    return `[]` for empty/whitespace input. The function must be PURE: no logging side
    effects that depend on state, no file/config/network access (a module `logger` may
    exist but must not be required for behavior). Add a Google-style docstring with full
    param/return annotations and a "no I/O" note. In exceptions.py, only add a new
    `NotesOSError` subclass IF you genuinely need one (the extractor itself should not
    raise on bad input — it returns []); if not needed, leave exceptions.py UNCHANGED and
    remove it from this task's edits. Run `ruff format` then `ruff check` then `mypy src`,
    then the focused pytest. Commit: `feat(05-01): pure heuristic extract_tasks + ExtractedTask model`.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/sorter/test_extractor.py -q && .pixi/envs/default/bin/ruff check src/notes_os/sorter/extractor.py && .pixi/envs/default/bin/mypy src/notes_os/sorter/extractor.py</automated>
  </verify>
  <done>All Task 1 tests pass; extractor.py is pure (no I/O), defines frozen ExtractedTask + extract_tasks; ruff and mypy clean on the module.</done>
</task>

<task type="auto">
  <name>Task 3: Register extractor in pyproject mypy override + verify coverage floor</name>
  <files>pyproject.toml</files>
  <action>
    Because `ExtractedTask` is a Pydantic BaseModel, append `"notes_os.sorter.extractor"`
    to the existing `[[tool.mypy.overrides]]` `module = [...]` list in pyproject.toml
    (current entries: notes_os.sorter.models, notes_os.backup_models, notes_os.config,
    notes_os.sorter.router, notes_os.sorter.ui, notes_os.sorter.session). Do NOT change
    `disallow_any_explicit = false` — just extend the module list (this mirrors how every
    prior BaseModel module was handled). Then run full `mypy src` (must be zero errors) and
    the focused extractor coverage check; the extractor module must be ≥90% covered and the
    overall suite must stay ≥80%. Discard pixi.lock churn afterward. Commit:
    `chore(05-01): add notes_os.sorter.extractor to mypy BaseModel override`.
  </action>
  <verify>
    <automated>grep -q '"notes_os.sorter.extractor"' pyproject.toml && .pixi/envs/default/bin/mypy src && .pixi/envs/default/bin/pytest tests/sorter/test_extractor.py --cov=notes_os.sorter.extractor --cov-report=term-missing --cov-fail-under=90 -q</automated>
  </verify>
  <done>pyproject.toml mypy override includes notes_os.sorter.extractor; `mypy src` is zero-error; extractor coverage ≥90%; overall suite still ≥80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note body text → extractor | Note body originates from Apple Notes (user-authored) and is passed as a plain string to a pure regex scanner. No code execution, no file paths derived from it in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | `extract_tasks` regexes | mitigate | Use simple, non-backtracking alternations (no nested quantifiers) to avoid ReDoS on adversarial note bodies; operate on pre-split short fragments, not the whole body in one match. |
| T-05-02 | Tampering | regex false positives writing junk | accept | Extraction output is reviewed by the user (plan 05-02 selection UI) before anything is written; no auto-write in this plan. |
| T-05-SC | Tampering | pip installs | mitigate | No new dependencies introduced (stdlib `re` + existing pydantic only); slopcheck N/A — no package-manager install tasks in this plan. |
</threat_model>

<verification>
- `extract_tasks` returns ≥1 ExtractedTask for at least one example of EACH locked signal
  family (action phrase, named commitment, inline date) and [] for no-match/empty input.
- `extractor.py` is pure: tests pass with no tmp_path / no filesystem; calling twice on the
  same input yields equal results.
- `notes_os.sorter.extractor` present in pyproject mypy override; `mypy src` zero errors.
- Extractor module coverage ≥90%; overall suite ≥80%.
</verification>

<success_criteria>
Maps to phase Success Criterion 2 ("the extractor identifies at least one action item …
from a note containing known patterns"). All three signal families covered by tests; module
pure and registered in mypy override; coverage floors green.
</success_criteria>

<output>
Create `.planning/phases/05-task-extraction/05-01-SUMMARY.md` when done.
</output>
