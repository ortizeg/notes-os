---
phase: 04-sorting-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/notes_os/config.py
  - pyproject.toml
  - tests/test_config_unit.py
autonomous: true
requirements: [CONF-01, CONF-02]
must_haves:
  truths:
    - "load_config() with no file returns a SorterConfig built from frozen Pydantic V2 defaults"
    - "load_config(path) reads a TOML file and validates it into SorterConfig"
    - "A malformed/schema-invalid TOML raises a clear validation error, not a silent fallback"
    - "SorterConfig COMPOSES BridgeConfig + BackupConfig as nested fields (no field duplication)"
    - "SorterConfig exposes archive (base folder + auto_year), log_dir, and a features.task_extraction flag (default off)"
  artifacts:
    - path: "src/notes_os/config.py"
      provides: "SorterConfig, ArchiveConfig, FeaturesConfig, load_config(), ConfigError"
      contains: "class SorterConfig"
      min_lines: 90
    - path: "tests/test_config_unit.py"
      provides: "Unit tests for defaults, TOML load, malformed-TOML error, composition"
      contains: "def test_"
    - path: "pyproject.toml"
      provides: "mypy override entry for notes_os.config"
      contains: "notes_os.config"
  key_links:
    - from: "src/notes_os/config.py"
      to: "notes_os.sorter.models.BridgeConfig"
      via: "Field(default_factory=BridgeConfig)"
      pattern: "BridgeConfig"
    - from: "src/notes_os/config.py"
      to: "notes_os.backup_models.BackupConfig"
      via: "Field(default_factory=BackupConfig)"
      pattern: "BackupConfig"
    - from: "pyproject.toml"
      to: "notes_os.config"
      via: "[[tool.mypy.overrides]] module list"
      pattern: "notes_os\\.config"
---

<objective>
Fill the `src/notes_os/config.py` stub with the frozen Pydantic V2 `SorterConfig`
that COMPOSES the already-merged `BridgeConfig` and `BackupConfig`, adds the new
archive / log_dir / features groups the router + session + UI need, and provides a
`load_config()` TOML loader with sensible defaults and a clear error on malformed input.

Purpose: Establishes the single config object the rest of Phase 4 (router, ui,
session, integration controller) is constructed from. This is the wave-1 foundation
that all other Phase 4 plans depend on (CONF-01, CONF-02).

Output: `SorterConfig`, `ArchiveConfig`, `FeaturesConfig`, `load_config()`, `ConfigError`
in `config.py`; the `notes_os.config` mypy override entry in `pyproject.toml`; unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reuse these — do NOT redefine the sub-configs:
@src/notes_os/sorter/models.py   # BridgeConfig (inbox_folder, preview_length, para_folders)
@src/notes_os/backup_models.py   # BackupConfig (backup_dir, notes_db_dir, max_backups, auto_backup_on_write)
@src/notes_os/exceptions.py      # NotesOSError root — ConfigError must extend it
@src/notes_os/config.py          # current docstring-only stub to fill

# Existing mypy-override shape to extend:
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SorterConfig composing BridgeConfig + BackupConfig + new groups</name>
  <files>src/notes_os/config.py, tests/test_config_unit.py</files>
  <behavior>
    - SorterConfig() with no args constructs successfully; .bridge is a BridgeConfig with inbox_folder=="Notes", .backup is a BackupConfig with max_backups==10
    - SorterConfig().archive.base_folder == "Archive" and SorterConfig().archive.auto_year is True
    - SorterConfig().log_dir == Path.home()/".notes-os"/"logs"
    - SorterConfig().features.task_extraction is False
    - All four models are frozen: assigning to any field raises pydantic.ValidationError
    - SorterConfig nests BridgeConfig/BackupConfig as fields (NOT re-declared scalar fields) — composition not duplication (CONF-02)
  </behavior>
  <action>
    Fill the config.py stub (keep its module docstring; `from __future__ import annotations` stays
    first import). Define three frozen `BaseModel` sub-models with `model_config = ConfigDict(frozen=True)`:
    `ArchiveConfig` (base_folder: str = "Archive"; auto_year: bool = True), `FeaturesConfig`
    (task_extraction: bool = False — forward-compat for Phase 5, keep minimal, no other fields), and
    the top-level `SorterConfig`. SorterConfig COMPOSES the existing merged sub-configs as fields
    (CONF-02): `bridge: BridgeConfig = Field(default_factory=BridgeConfig)`,
    `backup: BackupConfig = Field(default_factory=BackupConfig)`,
    `archive: ArchiveConfig = Field(default_factory=ArchiveConfig)`,
    `features: FeaturesConfig = Field(default_factory=FeaturesConfig)`, and
    `log_dir: Path = Field(default_factory=lambda: Path.home() / ".notes-os" / "logs")`.
    Import BridgeConfig from notes_os.sorter.models and BackupConfig from notes_os.backup_models —
    do NOT redefine them. Define `ConfigError(NotesOSError)` in this module (extends the root from
    notes_os.exceptions) for use by the loader in Task 2. Google-style docstrings on every model and
    field group. pathlib only. Import `datetime` is NOT needed here. Write tests/test_config_unit.py
    asserting the behavior cases above: defaults, frozen-ness (pytest.raises(ValidationError) on
    assignment), and that bridge/backup are the real nested model instances (isinstance checks).
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/test_config_unit.py -q -m 'not integration'</automated>
  </verify>
  <done>SorterConfig + ArchiveConfig + FeaturesConfig + ConfigError exist; all frozen; defaults match; bridge/backup are nested model instances; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TOML loader with defaults + clear malformed-TOML error (CONF-01)</name>
  <files>src/notes_os/config.py, tests/test_config_unit.py</files>
  <behavior>
    - load_config(None) when ~/.notes-os/config.toml is ABSENT returns SorterConfig() defaults (mock Path.home or pass a non-existent explicit path)
    - load_config(tmp_path/"config.toml") with valid TOML (e.g. [bridge] inbox_folder="Inbox", preview_length=300) returns a SorterConfig whose bridge.inbox_folder=="Inbox", bridge.preview_length==300
    - load_config(path) with a syntactically broken TOML raises ConfigError whose message names the file path
    - load_config(path) with a schema-invalid value (e.g. preview_length=10, below the ge=50 floor) raises a clear error (pydantic ValidationError or ConfigError wrapping it) — NOT a silent default fallback (SC1)
  </behavior>
  <action>
    Add `load_config(path: Path | None = None) -> SorterConfig` to config.py (CONF-01). When `path` is
    None, default to `Path.home() / ".notes-os" / "config.toml"`. If the resolved path does NOT exist,
    return `SorterConfig()` (defaults). If it exists, read bytes and parse with `tomllib.load` (import
    `tomllib` — 3.11 stdlib; open the file in binary mode via `Path.open("rb")` per PTH). Wrap a
    `tomllib.TOMLDecodeError` in `ConfigError` with a message that includes the file path so the user
    knows which file is malformed. Validate the parsed dict into SorterConfig via
    `SorterConfig.model_validate(data)`. Let pydantic.ValidationError propagate (acceptable per locked
    decision) OR wrap it in ConfigError with the path for a clearer message — either is fine, but it
    MUST surface as an error, never a silent fallback to defaults (SC1). Use logging (logger =
    logging.getLogger(__name__)) for an info line on which config source was loaded; zero print().
    Add tests covering all four behavior cases (use tmp_path for the file cases; monkeypatch Path.home
    or pass an explicit nonexistent path for the absent case).
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/test_config_unit.py -q -m 'not integration' && .pixi/envs/default/bin/python -c "from notes_os.config import load_config; c=load_config(); print(c.bridge.inbox_folder, c.archive.base_folder, c.log_dir)"</automated>
  </verify>
  <done>load_config returns defaults when file absent, parses + validates when present, and raises ConfigError/ValidationError (never silent) on malformed or schema-invalid TOML. CONF-01 + SC1 proven.</done>
</task>

<task type="auto">
  <name>Task 3: Register notes_os.config in the mypy explicit-Any override + verify gates</name>
  <files>pyproject.toml</files>
  <action>
    config.py defines Pydantic BaseModels, which inherit explicit `Any` from the BaseModel API and
    trip the GLOBAL `disallow_any_explicit = true`. Append `"notes_os.config"` to the existing
    `[[tool.mypy.overrides]]` `module = [...]` list (currently
    `["notes_os.sorter.models", "notes_os.backup_models"]`) so it becomes
    `["notes_os.sorter.models", "notes_os.backup_models", "notes_os.config"]`. This single override
    entry covers ArchiveConfig/FeaturesConfig/SorterConfig since they all live in config.py. Do NOT
    touch any other pyproject section. NOTE for wave coordination: this plan is the SOLE owner of the
    mypy-override edit in wave 1; router/session plans that also define models will append their own
    module names in wave 2 (serialized after this plan by file-overlap on pyproject.toml). After
    editing, run ruff + mypy + the config tests. ENVIRONMENT: the `pixi` shell function is BROKEN —
    use the `.pixi/envs/default/bin/*` binaries directly. pre-commit is NOT installed, so run ruff
    format + ruff check + mypy MANUALLY here. If pixi.lock shows self-version churn, discard it with
    `git checkout -- pixi.lock`.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src/notes_os/config.py tests/test_config_unit.py && .pixi/envs/default/bin/ruff check src/notes_os/config.py tests/test_config_unit.py && .pixi/envs/default/bin/mypy src/notes_os/config.py && grep -q 'notes_os.config' pyproject.toml</automated>
  </verify>
  <done>mypy strict passes on config.py with zero [explicit-any] errors; ruff clean; "notes_os.config" present in the mypy override module list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app | `~/.notes-os/config.toml` is user-authored untrusted input parsed into config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `config.toml` malformed/hostile values | mitigate | Parse via stdlib `tomllib` (no code eval) + validate through frozen Pydantic SorterConfig; out-of-range values (e.g. preview_length<50) rejected by field constraints; malformed TOML wrapped in ConfigError with path |
| T-04-02 | Denial of Service | huge/garbage config file | accept | Local single-user file the user controls; tomllib bounds parsing; no network input |
| T-04-SC | Tampering | pip installs | accept | No new packages installed in this plan (tomllib is 3.11 stdlib; pydantic/rich/readchar already declared) |
</threat_model>

<verification>
- `load_config()` returns defaults with no file; parses + validates with a file; errors clearly on malformed/invalid input (SC1).
- SorterConfig composes BridgeConfig + BackupConfig as nested fields (CONF-02) — no scalar duplication.
- mypy strict + ruff full ruleset green on config.py; `notes_os.config` in mypy override.
</verification>

<success_criteria>
- CONF-01: config loads from `~/.notes-os/config.toml` when present, falls back to defaults otherwise.
- CONF-02: frozen Pydantic V2 models with validated fields, composing the existing sub-configs.
- SC1 provable: malformed TOML raises a clear validation/config error with the file path.
</success_criteria>

<output>
Create `.planning/phases/04-sorting-core/04-01-SUMMARY.md` when done.
</output>
