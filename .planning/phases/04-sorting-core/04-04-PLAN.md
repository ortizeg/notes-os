---
phase: 04-sorting-core
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/notes_os/sorter/session.py
  - pyproject.toml
  - tests/sorter/test_session_unit.py
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03]
must_haves:
  truths:
    - "A session tracks moved / skipped / error counts as triage progresses (SESS-01)"
    - "The session produces an end-of-session summary of moved/skipped/error counts (SESS-02)"
    - "A session log is written to ~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log (SESS-03)"
  artifacts:
    - path: "src/notes_os/sorter/session.py"
      provides: "SortSession tracker (record_move/skip/error), frozen SessionSummary, write_log(log_dir)"
      contains: "class SortSession"
      min_lines: 80
    - path: "tests/sorter/test_session_unit.py"
      provides: "Tests for counts, summary, and log-file write to tmp log_dir with injected clock"
      contains: "def test_"
    - path: "pyproject.toml"
      provides: "mypy override entry for notes_os.sorter.session"
      contains: "notes_os.sorter.session"
  key_links:
    - from: "src/notes_os/sorter/session.py"
      to: "filesystem log_dir"
      via: "Path(log_dir).mkdir + (log_dir / 'YYYY-MM-DD_HHMMSS.log').write_text"
      pattern: "log_dir"
    - from: "src/notes_os/sorter/session.py"
      to: "SorterConfig.log_dir"
      via: "controller passes config.log_dir into write_log"
      pattern: "write_log"
---

<objective>
Build session tracking in `src/notes_os/sorter/session.py`: a `SortSession` that
records moved / skipped / error events during triage, produces a frozen
`SessionSummary` of the counts, and writes a session log to
`~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log` (log_dir from config).

Purpose: SESS-01..03. Gives the controller (04-05) a single place to accumulate
outcome counts and persist an audit log of the session, satisfying SC5.

Output: `SortSession` (record_move/record_skip/record_error), frozen `SessionSummary`,
`write_log(log_dir)` using pathlib + an injectable clock; mypy override entry; tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/notes_os/sorter/models.py   # Note (for typing the moved-note context if recorded)
@.planning/phases/04-sorting-core/04-01-SUMMARY.md   # SorterConfig.log_dir default ~/.notes-os/logs
@.planning/phases/04-sorting-core/04-02-SUMMARY.md   # RouteResult action types (MOVE/SKIP) the controller maps to record_* calls
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SortSession counters + frozen SessionSummary (SESS-01/02)</name>
  <files>src/notes_os/sorter/session.py, tests/sorter/test_session_unit.py</files>
  <behavior>
    - A fresh SortSession reports moved==0, skipped==0, errors==0
    - record_move() three times, record_skip() twice, record_error() once → summary moved==3, skipped==2, errors==1
    - summary() returns a frozen SessionSummary; assigning to a field raises pydantic.ValidationError
    - total/processed accessor (if provided) == moved+skipped+errors
  </behavior>
  <action>
    Module docstring first, then `from __future__ import annotations`. Define a frozen `SessionSummary`
    BaseModel (ConfigDict(frozen=True)) with `moved: int`, `skipped: int`, `errors: int` (Field ge=0)
    and a `started_at`/`ended_at` if cheap (optional). Define `SortSession` (a plain class, NOT a
    frozen model — it mutates counters) with internal counters and methods `record_move(...)`,
    `record_skip(...)`, `record_error(...)` (SESS-01) and `summary() -> SessionSummary` (SESS-02).
    Optionally record per-note detail (note id + resolved path / "skipped" / error message) in an
    internal list for the log in Task 2. Google-style docstrings; logging not print; no global state.
    Write tests asserting the behavior cases above incl. frozen-ness of SessionSummary.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/sorter/test_session_unit.py -q -m 'not integration' -k 'count or summary'</automated>
  </verify>
  <done>SortSession tracks moved/skipped/error counts and emits a frozen SessionSummary; tests pass. SESS-01 + SESS-02 proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_log to ~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log + mypy override (SESS-03)</name>
  <files>src/notes_os/sorter/session.py, tests/sorter/test_session_unit.py, pyproject.toml</files>
  <behavior>
    - write_log(tmp_path, now=fixed_dt) creates tmp_path/"2031-01-02_03-04-05.log" (filename from the injected clock, exact YYYY-MM-DD_HHMMSS format)
    - The log file contents include the moved/skipped/error counts and the per-note outcome lines
    - write_log creates the log_dir if it does not exist (mkdir parents=True, exist_ok=True)
    - Returns the Path written so the controller can log/report it
  </behavior>
  <action>
    Add `write_log(self, log_dir: Path, now: datetime | None = None) -> Path` to SortSession (SESS-03).
    Inject the clock: `now` defaults to `datetime.now()` at call time (RUNTIME default is allowed;
    tests pass a fixed datetime for determinism — do NOT bake datetime.now() into a frozen default).
    Format the filename EXACTLY as `now.strftime("%Y-%m-%d_%H-%M-%S") + ".log"` (SESS-03 pattern).
    `Path(log_dir).mkdir(parents=True, exist_ok=True)` then write the summary counts + per-note outcome
    lines via `Path.write_text` (pathlib only, never os.path; never print). Return the written Path.
    Tests: pass tmp_path as log_dir and a fixed datetime, assert the exact filename, that the file
    exists, and that contents contain the counts. THEN register the model module: session.py defines a
    `SessionSummary` BaseModel, so append `"notes_os.sorter.session"` to the `[[tool.mypy.overrides]]`
    `module` list in pyproject.toml. SHARED-FILE NOTE: pyproject.toml's mypy override is appended by
    04-01 (wave 1: notes_os.config), 04-02 (wave 2: notes_os.sorter.router), and THIS plan (wave 3:
    notes_os.sorter.session) — each in a strictly later wave so the edits never collide. Append ONLY
    your module name to the END of the existing list; do not reorder or remove prior entries. The list
    after this plan: `["notes_os.sorter.models", "notes_os.backup_models", "notes_os.config",
    "notes_os.sorter.router", "notes_os.sorter.session"]`. ENVIRONMENT: pixi shim BROKEN → use
    `.pixi/envs/default/bin/*`; pre-commit NOT installed → run ruff format + ruff check + mypy
    MANUALLY before commit; discard pixi.lock churn with `git checkout -- pixi.lock`.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src/notes_os/sorter/session.py tests/sorter/test_session_unit.py && .pixi/envs/default/bin/ruff check src/notes_os/sorter/session.py tests/sorter/test_session_unit.py && .pixi/envs/default/bin/mypy src/notes_os/sorter/session.py && .pixi/envs/default/bin/pytest tests/sorter/test_session_unit.py -q -m 'not integration' --cov=notes_os.sorter.session --cov-report=term-missing --cov-fail-under=85</automated>
  </verify>
  <done>write_log writes ~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log (exact format, injectable clock) with counts + outcomes; mypy override updated; mypy/ruff clean; session.py coverage ≥85%. SESS-03 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → filesystem | session log written under ~/.notes-os/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Repudiation | no record of what was moved | mitigate | Session log persists per-note outcomes + counts to a timestamped file (SESS-03), providing an audit trail of the triage session |
| T-04-08 | Information Disclosure | note content in log file | accept | Log lives under the user's own ~/.notes-os/logs and records the user's own data locally; no network egress |
| T-04-SC | Tampering | pip installs | accept | No new packages installed (stdlib datetime/pathlib; pydantic already declared) |
</threat_model>

<verification>
- moved/skipped/error counts accumulate correctly and surface in a frozen SessionSummary (SESS-01/02).
- write_log produces ~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log with exact timestamp format via injected clock (SESS-03).
- mypy override updated for notes_os.sorter.session; mypy/ruff clean; coverage ≥85%.
</verification>

<success_criteria>
- SESS-01..03 covered; SC5 (summary of counts + log file at ~/.notes-os/logs/YYYY-MM-DD_HHMMSS.log) provable.
</success_criteria>

<output>
Create `.planning/phases/04-sorting-core/04-04-SUMMARY.md` when done.
</output>
