---
phase: 04-sorting-core
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/notes_os/sorter/ui.py
  - tests/sorter/test_ui_unit.py
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "Each note renders title + its Markdown preview (Note.preview, already HTML-stripped) via Rich for Warp (UI-01)"
    - "A single category keystroke is captured without Enter, case-insensitive, via readchar (UI-02)"
    - "Numbered folder/subfolder lists render and a number+Enter selection is read (UI for ROUT-03/04/05)"
    - "? shows an inline PARA quick-reference overlay without leaving the flow (UI-03)"
    - "Session start shows the inbox note count (UI-04)"
  artifacts:
    - path: "src/notes_os/sorter/ui.py"
      provides: "SortUIProtocol + RichSortUI (render_note, prompt_category, prompt_choice, show_help, show_inbox_count) + FakeUI-friendly seams"
      contains: "class RichSortUI"
      min_lines: 90
    - path: "tests/sorter/test_ui_unit.py"
      provides: "Tests with injected fake readchar/console seam (no real keyboard)"
      contains: "def test_"
  key_links:
    - from: "src/notes_os/sorter/ui.py"
      to: "rich.markdown.Markdown / rich.console.Console"
      via: "render preview as Markdown to a Console"
      pattern: "Markdown|Console"
    - from: "src/notes_os/sorter/ui.py"
      to: "readchar.readkey"
      via: "single-keystroke capture (injectable for tests)"
      pattern: "readchar|readkey"
---

<objective>
Build the THIN Rich/readchar terminal layer in `src/notes_os/sorter/ui.py`: render a
note's title + Markdown preview, capture a single category keystroke without Enter,
render numbered folder/subfolder lists and read number+Enter, show a `?` PARA help
overlay inline, and show the inbox count at session start. Define a `SortUIProtocol`
so the integration controller (04-05) and tests can inject a fake UI.

Purpose: UI-01..04. This is the presentation/input boundary — it holds NO routing
logic (that lives in the router from 04-02). Keeping it thin and behind a Protocol
keeps the controller testable with a FakeUI and the real UI ~70% covered.

Output: `SortUIProtocol` interface, `RichSortUI` implementation, unit tests using an
injected fake input/console seam (no real keyboard, no real terminal).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Data the UI renders (Note.preview is ALREADY HTML-stripped + truncated by the bridge):
@src/notes_os/sorter/models.py   # Note(title, preview), ParaStructure
@.planning/phases/04-sorting-core/04-01-SUMMARY.md   # SorterConfig (preview length lives in bridge config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SortUIProtocol + RichSortUI render_note (Markdown preview) + show_inbox_count</name>
  <files>src/notes_os/sorter/ui.py, tests/sorter/test_ui_unit.py</files>
  <action>
    Module docstring first, then `from __future__ import annotations`. Define a `SortUIProtocol`
    (typing.Protocol, @runtime_checkable) with the methods the controller needs:
    `show_inbox_count(count: int) -> None`, `render_note(note: Note) -> None`,
    `prompt_category() -> str`, `prompt_choice(options: Sequence[str]) -> int | None` (returns the
    chosen 1-based index or None on back/invalid), `show_help() -> None`, and a
    `show_summary(...)` seam left for 04-04 to render the session summary (accept a generic mapping or
    a session summary object — coordinate the exact type with 04-04 via its SUMMARY; keep the method
    present). Implement `RichSortUI` against the Protocol. For `render_note` (UI-01): render the
    note.title as a Rich heading/Panel and render `note.preview` as Markdown via
    `rich.markdown.Markdown` printed to an injected `rich.console.Console` (Warp renders the styled
    output). Note.preview is ALREADY HTML-stripped + length-truncated by the bridge — do NOT re-strip
    or re-truncate; just render it. `show_inbox_count` (UI-04) prints "Inbox: N notes" style line at
    session start via the Console. Inject the `Console` (default Console()) and a keystroke reader
    callable on the constructor so tests pass fakes — RichSortUI must NOT hardcode stdout/keyboard.
    Zero print() — use the Rich Console and `logging` only. Write tests in test_ui_unit.py: inject a
    Console with `record=True` (or a StringIO file=) and assert render_note emits the title and
    preview text; assert show_inbox_count emits the count.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/pytest tests/sorter/test_ui_unit.py -q -m 'not integration' -k 'render or count'</automated>
  </verify>
  <done>SortUIProtocol defined; RichSortUI.render_note renders title + Markdown preview to an injected Console; show_inbox_count renders the count; tests pass with a recording Console (no real terminal). UI-01 + UI-04 proven.</done>
</task>

<task type="auto">
  <name>Task 2: Single-keystroke category capture + numbered choice + ? help overlay</name>
  <files>src/notes_os/sorter/ui.py, tests/sorter/test_ui_unit.py</files>
  <action>
    Implement `prompt_category` (UI-02): read ONE keystroke without Enter via the injected reader
    (default `readchar.readkey`), normalize to a single lower-cased character, and return it — NO
    routing decision here (the router interprets it). Implement `prompt_choice(options)` (UI for
    ROUT-03/04/05): render the options as a numbered list (1-based) via the Console, then read a line
    (number + Enter) from the injected input; return the parsed 1-based index, or None for a back
    request ('b'/'B') or invalid/non-numeric/out-of-range input so the controller/router can re-prompt
    or back out. Implement `show_help` (UI-03): render an inline PARA quick-reference overlay
    (P=Projects, A=Areas, R=Resources, X=Archive, S=Skip, B=Back, ?=Help) to the Console WITHOUT
    exiting the flow — it returns and the caller re-prompts. Keep the UI thin: it contains no PARA
    routing logic, only capture + render. Inject BOTH the single-key reader and the line reader so
    tests feed scripted input (e.g. reader returns 'p', line reader returns "2\n"); assert
    prompt_category lower-cases and returns the key, prompt_choice returns the right index and None on
    'b'/garbage, and show_help emits the quick-reference text. ENVIRONMENT: pixi shim BROKEN → use
    `.pixi/envs/default/bin/*`; pre-commit NOT installed → run ruff format + ruff check + mypy
    MANUALLY (esp. ruff format on the new test file); discard pixi.lock churn with
    `git checkout -- pixi.lock`. readchar is already a declared dep (>=4.0) — tests MUST NOT block on
    real keyboard input; always use the injected fake reader.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src/notes_os/sorter/ui.py tests/sorter/test_ui_unit.py && .pixi/envs/default/bin/ruff check src/notes_os/sorter/ui.py tests/sorter/test_ui_unit.py && .pixi/envs/default/bin/mypy src/notes_os/sorter/ui.py && .pixi/envs/default/bin/pytest tests/sorter/test_ui_unit.py -q -m 'not integration'</automated>
  </verify>
  <done>prompt_category captures one key without Enter (case-insensitive), prompt_choice reads number+Enter returning index/None, show_help renders the PARA reference inline; all via injected fakes (no real keyboard); mypy/ruff clean. UI-02 + UI-03 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard → UI | raw keystrokes/lines (untrusted) captured at the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | malformed keystroke/line input | mitigate | prompt_choice parses defensively (non-numeric/out-of-range → None); prompt_category returns a single normalized char; the router (not the UI) validates semantics |
| T-04-06 | Information Disclosure | rendering note preview content | accept | Preview is the user's own note already truncated by the bridge; rendered locally to the user's terminal only |
| T-04-SC | Tampering | pip installs | accept | No new packages installed (rich + readchar already declared in pyproject) |
</threat_model>

<verification>
- render_note shows title + Markdown-rendered preview (no re-strip/re-truncate) to an injectable Console (UI-01).
- prompt_category captures one keystroke without Enter, case-insensitive (UI-02); prompt_choice reads number+Enter (ROUT-03/04/05 UI); show_help renders inline (UI-03); show_inbox_count at start (UI-04).
- UI is thin (no routing logic) and behind SortUIProtocol so a FakeUI can be injected in 04-05; mypy/ruff clean. No pyproject mypy-override edit (UI defines no BaseModel).
</verification>

<success_criteria>
- UI-01..04 covered. SortUIProtocol enables a fake UI for the controller's full-loop test in 04-05.
- Tests never block on real keyboard input (injected reader); RichSortUI ~70% covered.
</success_criteria>

<output>
Create `.planning/phases/04-sorting-core/04-03-SUMMARY.md` when done.
</output>
