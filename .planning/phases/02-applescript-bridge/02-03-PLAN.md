---
phase: 02-applescript-bridge
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - tests/sorter/test_notes_unit.py
  - tests/sorter/test_notes_integration.py
autonomous: true
requirements: [BRDG-01, BRDG-02, BRDG-03, BRDG-04, BRDG-05, BRDG-06, BRDG-07]
must_haves:
  truths:
    - "A mocked unit test retrieves inbox notes via NotesRepositoryProtocol using MockNotesRepository with no AppleScript called"
    - "Mocked unit tests exercise every public function of AppleScriptNotesRepository (read, move, ensure, HTML strip, errors) with subprocess.run patched"
    - "Injecting a failing osascript stub raises NotesError / FolderNotFoundError / NotesMoveError and is catchable as NotesOSError (session continues)"
    - "notes.py reaches >=95% coverage from the mocked unit suite alone (integration tests excluded from CI)"
    - "Integration tests are all marked @pytest.mark.integration and use a dedicated _TestInbox folder, never touching real notes"
  artifacts:
    - path: "tests/sorter/test_notes_unit.py"
      provides: "Mocked unit suite covering all bridge public functions and error paths"
      contains: "def test_get_inbox_notes"
    - path: "tests/sorter/test_notes_integration.py"
      provides: "macOS-only integration suite using _TestInbox (excluded from CI)"
      contains: "@pytest.mark.integration"
  key_links:
    - from: "tests/sorter/test_notes_unit.py"
      to: "subprocess.run"
      via: "unittest.mock.patch on osascript execution"
      pattern: "patch.*subprocess"
    - from: "tests/sorter/test_notes_unit.py"
      to: "tests/sorter/conftest.py"
      via: "consumes MockNotesRepository + sample fixtures"
      pattern: "mock_repo|sample_notes"
---

<objective>
Prove the bridge contract works and drive `notes.py` to >=95% coverage using mocked
unit tests that run in CI, plus a macOS-only integration suite for local validation.

Purpose: Satisfy all five phase success criteria — the mock-based Protocol read
(SC1), real round-trip move via integration (SC2), idempotent ensure_folder (SC3),
typed-error-without-crash (SC4), and notes.py >=95% branch coverage in CI achieved by
the mocked unit suite ALONE (SC5). Integration tests are excluded from CI (`pytest -m
'not integration'`) so coverage must NOT depend on them.

Output:
- `tests/sorter/test_notes_unit.py` — all `subprocess.run` mocked; covers every public
  function and every error branch of `AppleScriptNotesRepository`, the `_HTMLStripper`/
  `_strip_html` cases, and a MockNotesRepository-via-Protocol read (no AppleScript).
- `tests/sorter/test_notes_integration.py` — all `@pytest.mark.integration`, macOS-only,
  dedicated `_TestInbox` folder, create/teardown fixtures, real read / structure /
  move / ensure_folder / round-trip.

Depends on 02-01 (contracts + osascript format + seam) and 02-02 (write ops, errors,
MockNotesRepository + fixtures in conftest.py).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

# Contracts, osascript output format, and the layer where sentinels are detected
@.planning/phases/02-applescript-bridge/02-01-SUMMARY.md
@.planning/phases/02-applescript-bridge/02-02-SUMMARY.md
# The module under test and its test double
@src/notes_os/sorter/notes.py
@src/notes_os/sorter/models.py
@tests/sorter/conftest.py
@src/notes_os/exceptions.py
# Existing test style reference (monkeypatch, parametrize, no print)
@tests/test_app.py
@tests/test_package_imports.py
</context>

<mocking_strategy>
ALL osascript I/O in the unit suite is mocked — no subprocess ever spawns. Patch the
single execution point. Two equivalent options; pick the one matching how 02-01 wrote
`_run_osascript`:

- Preferred: `unittest.mock.patch("notes_os.sorter.notes.subprocess.run")` and return a
  `types.SimpleNamespace(returncode=0, stdout=..., stderr="")` (or a configured
  `MagicMock`) so the real `_run_osascript` parsing/return logic is exercised (keeps
  that wrapper's branches in coverage).
- Build helper(s) in the test module to construct fake osascript stdout using the SAME
  `_FIELD_SEP`/`_RECORD_SEP` constants documented in 02-01-SUMMARY (import the constants
  from notes_os.sorter.notes rather than hardcoding chr(31)/chr(30)).

For non-zero / failure paths: return `returncode=1, stderr="...permission denied..."`
or stdout/stderr containing the `FOLDER_NOT_FOUND` / `NOTE_NOT_FOUND` sentinels (per the
layer 02-02-SUMMARY says detects them) to assert the typed exception raised.

Use the conftest MockNotesRepository + fixtures (sample_notes, sample_structure,
mock_repo) for the Protocol-level test that proves no AppleScript is called.
</mocking_strategy>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mocked unit suite — read ops, HTML stripping, Protocol/mock read</name>
  <files>tests/sorter/test_notes_unit.py</files>
  <behavior>
    - get_inbox_notes (subprocess mocked): well-formed multi-record stdout → list[Note] with correct id/title and stripped preview; empty stdout → []; a note with empty body → preview ""; a title containing apostrophes/quotes/Unicode survives the US/RS round-trip; non-zero returncode → NotesError; permission-denied stderr → NotesError; malformed/short record handled (documented behavior: skipped or raises — assert whichever 02-01 implemented).
    - get_para_structure (subprocess mocked): all four roots populated; subfolders mapped to correct parent; a folder with no subfolders → () ; a root with no children still present; non-zero returncode → NotesError.
    - HTML stripping: strips div/p/br/b/i/ul/li; preserves text; decodes entities; truncates AFTER stripping; empty input → ""; plain-text passthrough.
    - Protocol read via MockNotesRepository: get_inbox_notes() returns seeded notes and subprocess.run is asserted NOT called (patched + assert_not_called) — proving SC1 (no AppleScript).
  </behavior>
  <action>
    Create tests/sorter/test_notes_unit.py with a module docstring then
    `from __future__ import annotations`. Import: `from unittest.mock import patch`,
    `types` (or MagicMock), pytest, the repository + Protocol + _strip_html + the
    `_FIELD_SEP`/`_RECORD_SEP` constants from notes_os.sorter.notes, the models, and the
    typed exceptions. (tests/** already get S101/S105/S106 ignored per pyproject — asserts
    are fine.)

    Add a small helper to build fake osascript stdout from a list of records using the
    imported separators. Write tests covering EVERY get_inbox_notes case in the behavior
    block (use a fresh `AppleScriptNotesRepository(BridgeConfig())`, patch
    `notes_os.sorter.notes.subprocess.run` to return the crafted namespace). Use
    `pytest.raises(NotesError)` for failure paths. Parametrize the HTML-stripping cases
    against `_strip_html` directly (tags, entities, empty, passthrough, truncation, block
    separation). Write the get_para_structure cases similarly.

    Add ONE test that constructs the conftest `mock_repo` (or builds MockNotesRepository
    from sample_notes/sample_structure), calls `get_inbox_notes()` through the
    NotesRepositoryProtocol-typed variable, asserts the returned notes match the seed, AND
    asserts `subprocess.run` (patched at notes_os.sorter.notes.subprocess.run) was never
    called — this is the SC1 "no AppleScript" proof.

    Run `pixi run pytest tests/sorter/test_notes_unit.py -q` — all pass.
  </action>
  <verify>
    <automated>pixi run pytest tests/sorter/test_notes_unit.py -q</automated>
  </verify>
  <done>All read-op, HTML-stripping, and Protocol/mock-read tests pass with subprocess fully mocked; the SC1 test asserts subprocess.run was not called; failure paths assert NotesError.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mocked unit suite — write ops, error mapping, 95% coverage gate</name>
  <files>tests/sorter/test_notes_unit.py</files>
  <behavior>
    - move_note (subprocess mocked): 2-level path issues a move and the script contains `folder Web of folder Projects`; 3-level path nests correctly; success returns None.
    - move_note error mapping: stdout/stderr with FOLDER_NOT_FOUND → FolderNotFoundError; NOTE_NOT_FOUND → NotesMoveError; other non-zero returncode → NotesError. move_note does NOT call ensure_folder (assert via patching/spy on ensure_folder).
    - ensure_folder (subprocess mocked): creates when missing; no-op when exists; nested path (Archive/2026); non-zero returncode → NotesError.
    - Failing-stub resilience (SC4): a helper that injects a failing osascript (returncode=1) raises the typed error AND the caller can `except NotesOSError` and continue — assert the exception is a NotesOSError subclass so a session catching NotesOSError keeps running.
    - Coverage: notes.py >= 95% from this suite + Task 1 (CI runs `-m 'not integration'`).
  </behavior>
  <action>
    Append to tests/sorter/test_notes_unit.py. Patch `notes_os.sorter.notes.subprocess.run`
    for each case. For the script-content assertions, capture the patched call args
    (`mock_run.call_args`) and assert the AppleScript string contains the expected nested
    `folder ... of folder ...` reference for 2-level and 3-level FolderPaths.

    Error-mapping tests: configure the mock to return the FOLDER_NOT_FOUND / NOTE_NOT_FOUND
    sentinels (in stdout or stderr per 02-02-SUMMARY's documented layer) or returncode=1,
    and assert FolderNotFoundError / NotesMoveError / NotesError respectively via
    pytest.raises. Add a test asserting move_note does not call ensure_folder: patch
    `AppleScriptNotesRepository.ensure_folder` (or spy) and assert_not_called after a move.

    ensure_folder tests: assert idempotent no-op when the existence check reports the path
    exists (mock stdout accordingly), creation when missing, nested Archive/2026 creation,
    and NotesError on returncode=1.

    SC4 resilience test: write a tiny `def _failing_run(*a, **k)` returning returncode=1 (or
    a stub raising), patch it in, call a bridge op inside `try/except NotesOSError`, assert
    the except branch is hit and that the raised type is a NotesOSError subclass — proving
    the session continues without crashing.

    After writing, run the FULL non-integration suite WITH coverage scoped to notes.py and
    assert >=95%. If below 95%, add targeted tests for the uncovered branches (read the
    coverage report's missing lines) — do NOT lower the threshold. Confirm the overall gate
    (`--cov=notes_os --cov-fail-under=80`) still passes.
  </action>
  <verify>
    <automated>pixi run pytest -m 'not integration' tests/sorter/test_notes_unit.py --cov=notes_os.sorter.notes --cov-report=term-missing --cov-fail-under=95 -q</automated>
  </verify>
  <done>move_note (2/3-level, success, FolderNotFoundError/NotesMoveError/NotesError mapping, no ensure_folder call) and ensure_folder (create/no-op/nested/NotesError) are covered with subprocess mocked; the SC4 failing-stub test proves NotesOSError-catchability; notes.py reaches >=95% coverage from the mocked suite alone; overall 80% gate stays green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: macOS integration suite (excluded from CI) using _TestInbox</name>
  <files>tests/sorter/test_notes_integration.py</files>
  <behavior>
    - All tests marked @pytest.mark.integration (so CI's `-m 'not integration'` skips them).
    - A module/fixture-level skip when not on macOS or when osascript is unavailable.
    - Fixture creates a dedicated _TestInbox folder + a known test note, and tears them down — real user notes are never read or moved.
    - Real read: get_inbox_notes against _TestInbox returns the seeded note with the expected title and stripped-plain-text preview.
    - Real structure: get_para_structure returns the PARA roots.
    - Real move: move the test note from _TestInbox to a target folder and assert it left _TestInbox and arrived at the target (SC2), then move it back (round-trip).
    - ensure_folder: create a target folder, assert it exists, idempotent second call, cleanup (SC3 against real Notes).
  </behavior>
  <action>
    Create tests/sorter/test_notes_integration.py with a module docstring then
    `from __future__ import annotations`. Add `pytestmark = pytest.mark.integration` at
    module level (every test inherits the marker). Add a guard:
    `pytest.mark.skipif(sys.platform != "darwin", ...)` plus a check that `osascript` is on
    PATH (shutil.which) — skip the module otherwise.

    Build a fixture that, via a real AppleScriptNotesRepository configured with
    `BridgeConfig(inbox_folder="_TestInbox")`, ensures a `_TestInbox` folder exists, creates
    a known test note in it (use a raw osascript `make new note` helper local to the test —
    acceptable to call osascript directly in the integration test setup), yields, then
    deletes the test note and the _TestInbox folder on teardown. NEVER target the user's real
    inbox or PARA notes — only _TestInbox and a disposable target folder created/cleaned by
    the test.

    Write the read, structure, move (assert-left-source + assert-arrived-target + move-back),
    and ensure_folder (create + verify exists + idempotent + cleanup) tests per the behavior
    block. Use the repository's own methods (not raw osascript) for the operations under test
    so this is a true end-to-end validation of the bridge.

    Verify the markers work: `pixi run pytest -m 'not integration' tests/sorter -q` collects
    these as deselected (does NOT run them). Do not require a macOS run in this plan's automated
    gate (CI has no Notes); the integration run is for local validation.
  </action>
  <verify>
    <automated>pixi run pytest -m 'not integration' tests/sorter -q && pixi run pytest --collect-only -m integration tests/sorter/test_notes_integration.py -q</automated>
  </verify>
  <done>test_notes_integration.py exists with module-level `pytest.mark.integration` and a darwin/osascript skip guard; CI's `-m 'not integration'` deselects every test in it; the file defines _TestInbox setup/teardown and real read/structure/move/round-trip/ensure_folder tests touching only _TestInbox and a disposable target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → real Apple Notes (integration only) | Integration tests issue real moves/creates; a bug could touch real user data. |
| Mocked tests → no boundary | Unit tests never spawn subprocess; no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Integration tests against real Notes DB | mitigate | Every integration test confined to a dedicated `_TestInbox` folder + disposable target; setup/teardown create and remove only test-owned folders/notes; module-level `@pytest.mark.integration` keeps them out of CI; darwin/osascript skip guard prevents accidental runs elsewhere. |
| T-02-08 | Repudiation | A bridge failure silently passing tests | mitigate | Failure-path tests assert the SPECIFIC typed exception (FolderNotFoundError/NotesMoveError/NotesError) and NotesOSError-catchability, not just "raises". |
| T-02-SC | Tampering | pip installs | accept | No new packages — pytest + pytest-cov already in Phase 1 env (pixi.toml). No legitimacy gate required. |
</threat_model>

<verification>
- `pixi run pytest -m 'not integration' tests/sorter --cov=notes_os.sorter.notes --cov-fail-under=95` — passes (notes.py >=95% from mocked suite alone).
- `pixi run pytest -m 'not integration' --cov=notes_os --cov-fail-under=80` — overall gate green.
- `pixi run pytest --collect-only -m integration tests/sorter/test_notes_integration.py` — integration tests collected but deselected by the default `-m 'not integration'` addopts.
- `pixi run mypy` and `pixi run ruff` — clean on the new test files.
- SC1 proof present: a test asserts subprocess.run not called when reading via MockNotesRepository.
- SC4 proof present: failing-stub test catches NotesOSError and continues.
</verification>

<success_criteria>
- SC1 (BRDG-07): A unit test retrieves inbox notes via NotesRepositoryProtocol using MockNotesRepository with subprocess.run asserted not called.
- SC2 (BRDG-01,03): An integration test reads a real _TestInbox note (title + stripped preview), moves it to a target, asserts it left the inbox and arrived, and moves it back.
- SC3 (BRDG-04): ensure_folder called twice for the same path succeeds idempotently with no duplicate (covered in both mocked unit and integration suites).
- SC4 (BRDG-06): A failing osascript stub raises NotesError/FolderNotFoundError/NotesMoveError and the session continues (NotesOSError-catchable).
- SC5: notes.py passes mypy strict and reaches >=95% coverage in CI from the mocked unit suite alone (integration excluded).
</success_criteria>

<output>
Create `.planning/phases/02-applescript-bridge/02-03-SUMMARY.md` when done.
Record the final measured notes.py coverage percentage and, if any branches needed
extra targeted tests, which lines they were — so the verifier can confirm SC5 without
re-deriving it.
</output>
