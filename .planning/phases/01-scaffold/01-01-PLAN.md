---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - pixi.toml
  - .python-version
  - src/notes_os/__init__.py
  - src/notes_os/py.typed
  - src/notes_os/app.py
  - src/notes_os/config.py
  - src/notes_os/exceptions.py
  - src/notes_os/sorter/__init__.py
  - src/notes_os/distiller/__init__.py
  - src/notes_os/graph/__init__.py
  - src/notes_os/suggestions/__init__.py
  - tests/__init__.py
  - tests/sorter/__init__.py
  - tests/test_app.py
autonomous: true

must_haves:
  truths:
    - "`pixi run notes` resolves and runs from the repo root without error"
    - "`import notes_os.distiller`, `import notes_os.graph`, `import notes_os.suggestions` all succeed"
    - "`pixi run pytest -m 'not integration'` passes on the scaffold"
    - "The package is type-checkable (py.typed present, mypy strict clean)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Hatchling + hatch-vcs build, notes entry point, ruff/mypy/pytest config"
      contains: "notes = \"notes_os.app:main\""
    - path: "pixi.toml"
      provides: "pixi environment + task runner (notes, ruff, mypy, pytest)"
      contains: "[tasks]"
    - path: "src/notes_os/app.py"
      provides: "NotesOSApp placeholder + main() entry stub"
      contains: "def main"
    - path: "src/notes_os/distiller/__init__.py"
      provides: "M2 stub package"
    - path: "src/notes_os/graph/__init__.py"
      provides: "M3 stub package"
    - path: "src/notes_os/suggestions/__init__.py"
      provides: "M4 stub package"
    - path: "src/notes_os/py.typed"
      provides: "PEP 561 typing marker"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "src/notes_os/app.py:main"
      via: "entry point notes = notes_os.app:main"
      pattern: "notes_os\\.app:main"
    - from: "pixi.toml [tasks] notes"
      to: "notes entry point"
      via: "pixi run notes invokes the console script"
      pattern: "notes"
---

<objective>
Initialize the monorepo with a src layout, a build system (Hatchling + hatch-vcs), a pixi
environment + task runner, the `notes` console entry point, and stub packages for milestones
M2–M4 so the package structure is stable from day one.

Purpose: Establishes the foundation every other plan and phase builds on. `pixi run notes`
must resolve and run, and `import notes_os.distiller/graph/suggestions` must succeed without
installing extras (SCAF-01, SCAF-02).
Output: pyproject.toml, pixi.toml, .python-version, the full `src/notes_os/` package tree with
stubs, and a minimal passing test.
</objective>

<execution_context>
@/Users/ortizeg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ortizeg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Locked technical specs (implement EXACTLY):

pyproject.toml:
- build-system: requires = ["hatchling", "hatch-vcs"], build-backend = "hatchling.build"
- [project]: name = "para-notes-sorter", requires-python = ">=3.11", dynamic = ["version"]
- [project.scripts]: notes = "notes_os.app:main"
- [tool.hatch.version]: source = "vcs"
- [tool.hatch.build.targets.wheel]: packages = ["src/notes_os"]
- [tool.ruff]: line-length = 100, target-version = "py311", src = ["src"]
- [tool.ruff.lint]: select = ["E","F","I","N","UP","S","B","A","C4","T20","SIM","TCH","RUF","PTH","ERA"], ignore = ["E501"]
- [tool.ruff.lint.per-file-ignores]: "tests/**/*.py" = ["S101","S105","S106"]; "scripts/**" = ["T20"]
- [tool.ruff.lint.isort]: known-first-party = ["notes_os"], lines-after-imports = 2
- [tool.ruff.format]: quote-style = "double", indent-style = "space", docstring-code-format = true
- [tool.mypy]: python_version = "3.11", strict = true, warn_return_any = true, warn_unused_configs = true, disallow_any_generics = true, disallow_any_explicit = true, no_implicit_reexport = true, strict_equality = true, show_error_codes = true, show_column_numbers = true
- [tool.pytest.ini_options]: markers = ["integration: requires macOS with Apple Notes running", "slow: takes >5s"]; addopts default run excludes integration

Runtime deps (M1): rich >=13.0, textual >=0.80, readchar >=4.0, pydantic >=2.0. Stdlib only beyond these.

Coding standards: every .py file starts with `from __future__ import annotations`; full type
annotations; modern union syntax; Google-style docstrings; zero print() (T20 — use logging or the
Textual app); logging.getLogger(__name__); pathlib not os.path; no global state/magic numbers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pyproject.toml, pixi.toml, and .python-version</name>
  <files>pyproject.toml, pixi.toml, .python-version</files>
  <action>
Create `pyproject.toml` implementing the locked spec in <context> EXACTLY:
- [build-system] requires = ["hatchling", "hatch-vcs"], build-backend = "hatchling.build"
- [project]: name = "para-notes-sorter", requires-python = ">=3.11", dynamic = ["version"],
  description, readme (optional), and dependencies = ["rich>=13.0", "textual>=0.80",
  "readchar>=4.0", "pydantic>=2.0"]
- [project.scripts]: notes = "notes_os.app:main"
- [tool.hatch.version] source = "vcs"
- [tool.hatch.build.targets.wheel] packages = ["src/notes_os"]
- [tool.ruff], [tool.ruff.lint], [tool.ruff.lint.per-file-ignores], [tool.ruff.lint.isort],
  [tool.ruff.format] — all exactly as specified
- [tool.mypy] — all strict flags as specified
- [tool.pytest.ini_options]: markers as specified, and addopts = ["-m", "not integration"]
  so the default run excludes integration tests (CI default `pytest -m 'not integration'`).

Create `.python-version` containing exactly `3.11`.

Create `pixi.toml` defining the dev environment and task runner:
- [project] name = "para-notes-sorter", channels = ["conda-forge"], platforms = ["osx-arm64", "osx-64"]
- [dependencies]: python = ">=3.11,<3.13" (covers the 3.11/3.12 CI matrix)
- [pypi-dependencies]: install this project editable — `para-notes-sorter = { path = ".", editable = true }`
- [feature.dev.pypi-dependencies] or [pypi-dependencies]: ruff, mypy, pytest, pytest-cov, types-readchar
- [tasks]: `notes = "notes"` (invokes the console script), `ruff = "ruff check src tests"`,
  `format = "ruff format src tests"`, `mypy = "mypy src"`, `pytest = "pytest"`.

Prefer the pixi skill conventions. Use editable install so the `notes` entry point resolves
from source. Do NOT hand-set a version — hatch-vcs derives it from git tags; if no tag exists,
hatch-vcs falls back to a dev version, which is fine.
  </action>
  <verify>`pixi install` succeeds; `python -c "import tomllib,pathlib; tomllib.loads(pathlib.Path('pyproject.toml').read_text())"` parses; `python -c "import tomllib,pathlib; tomllib.loads(pathlib.Path('pixi.toml').read_text())"` parses.</verify>
  <done>pyproject.toml, pixi.toml, and .python-version exist and parse as valid TOML; pixi resolves the environment.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold src/notes_os package tree with M2-M4 stubs</name>
  <files>src/notes_os/__init__.py, src/notes_os/py.typed, src/notes_os/exceptions.py, src/notes_os/config.py, src/notes_os/sorter/__init__.py, src/notes_os/distiller/__init__.py, src/notes_os/graph/__init__.py, src/notes_os/suggestions/__init__.py</files>
  <action>
Create the package tree. Every `.py` file MUST begin with `from __future__ import annotations`
followed by a blank line, then a module-level Google-style docstring.

- `src/notes_os/__init__.py`: package docstring only (top-level package marker). Do NOT define
  `__version__` by hand — version comes from hatch-vcs metadata.
- `src/notes_os/py.typed`: empty file (PEP 561 marker).
- `src/notes_os/exceptions.py`: shared custom exception hierarchy base. Define a single
  `class NotesOSError(Exception)` with a docstring as the root of all project exceptions. Phase 2+
  subclasses extend this. Keep it minimal — just the base class now.
- `src/notes_os/config.py`: STUB. Module docstring noting config models arrive in Phase 4. No
  Pydantic models yet — an empty module with the docstring and the future-import is enough. Must
  pass mypy strict (so no undefined references).
- `src/notes_os/sorter/__init__.py`: package docstring — "PARA sorting subpackage; contents in
  Phase 2/4." Empty otherwise.
- `src/notes_os/distiller/__init__.py`: STUB (SCAF-02). Docstring: "M2 — Distillation Engine
  (stub; planned in a future milestone)."
- `src/notes_os/graph/__init__.py`: STUB (SCAF-02). Docstring: "M3 — Knowledge Graph (stub)."
- `src/notes_os/suggestions/__init__.py`: STUB (SCAF-02). Docstring: "M4 — Smart Suggestions (stub)."

All files must be ruff-clean and mypy-strict-clean.
  </action>
  <verify>`pixi run python -c "import notes_os, notes_os.sorter, notes_os.distiller, notes_os.graph, notes_os.suggestions, notes_os.config, notes_os.exceptions"` exits 0; `pixi run mypy` passes; `pixi run ruff` passes.</verify>
  <done>All subpackages import successfully without installing extras; mypy strict and ruff both clean.</done>
</task>

<task type="auto">
  <name>Task 3: Implement app.py entry point and minimal test</name>
  <files>src/notes_os/app.py, tests/__init__.py, tests/sorter/__init__.py, tests/test_app.py</files>
  <action>
Create `src/notes_os/app.py`:
- Begin with `from __future__ import annotations` and a module docstring.
- `import logging` and `logger = logging.getLogger(__name__)`.
- Define `class NotesOSApp:` as a documented placeholder. The full Textual `App` subclass arrives
  in Phase 6 — for now this is a minimal placeholder class with a docstring (it may hold a
  `run()` method that logs a banner). Do NOT add Textual mounting logic yet; keep it import-light
  so the entry point resolves instantly and CI stays fast.
- Define `def main() -> None:` — the console entry point. It MUST run without error in Phase 1.
  Implementation: configure a basic logger and emit a version/banner line via
  `logger.info(...)` (NOT print — T20 forbids print in src). Resolve the package version via
  `importlib.metadata.version("para-notes-sorter")` wrapped in try/except PackageNotFoundError,
  defaulting to "0.0.0+unknown" so it never raises. Log something like
  "NotesOS %s — TUI arrives in Phase 6" and return cleanly. The Textual app launch is wired in
  Phase 6; main() must not block or error now.
- Full type annotations; Google-style docstrings on the class and main().

Create `tests/__init__.py` (empty package marker) and `tests/sorter/__init__.py` (empty).

Create `tests/test_app.py`:
- `from __future__ import annotations`.
- Test that `notes_os.app.main()` runs without raising (call it directly; assert it returns None).
- Test that `NotesOSApp` can be instantiated.
- Use plain asserts (per-file-ignore allows S101 in tests).
  </action>
  <verify>`pixi run notes` runs and exits 0 (logs a banner, no traceback); `pixi run pytest` passes; `pixi run mypy` and `pixi run ruff` clean.</verify>
  <done>`pixi run notes` resolves through the entry point and runs without error; `tests/test_app.py` passes; mypy strict and ruff clean across src and tests.</done>
</task>

</tasks>

<verification>
- `pixi run notes` executes from repo root and exits 0 with no traceback (SCAF-01).
- `import notes_os.distiller`, `import notes_os.graph`, `import notes_os.suggestions` succeed (SCAF-02).
- `pixi run ruff`, `pixi run mypy`, `pixi run pytest -m 'not integration'` all pass on the scaffold.
- pyproject.toml exposes `notes = "notes_os.app:main"` and uses Hatchling + hatch-vcs.
</verification>

<success_criteria>
- SCAF-01 satisfied: monorepo + src layout + pixi env + Hatchling/hatch-vcs pyproject + `notes` entry point that resolves and runs.
- SCAF-02 satisfied: distiller/graph/suggestions stub packages import without extras.
- All three quality gates (ruff, mypy strict, pytest) pass on the scaffold — proving the gates CI will run in 01-02 are green from the start.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`.
</output>
