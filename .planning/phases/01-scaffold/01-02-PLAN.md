---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/lint.yml
  - .github/workflows/test.yml
  - .pre-commit-config.yaml
autonomous: true

must_haves:
  truths:
    - "A push to a feature branch triggers a CI matrix on macOS-latest × Python 3.11 and 3.12"
    - "CI runs three tiered jobs: lint (ruff check + ruff format --check), typecheck (mypy strict), and test (pytest)"
    - "Default CI test run excludes integration tests (`pytest -m 'not integration'`) and enforces `--cov-fail-under=80`"
    - "A commit with a ruff violation or a missing type annotation is rejected by pre-commit before reaching the remote"
  artifacts:
    - path: ".github/workflows/lint.yml"
      provides: "Separate lint (ruff) and typecheck (mypy strict) jobs on the macOS × py3.11/3.12 matrix"
      contains: "macos-latest"
    - path: ".github/workflows/test.yml"
      provides: "pytest job with coverage gate and codecov upload on the macOS × py3.11/3.12 matrix"
      contains: "cov-fail-under"
    - path: ".pre-commit-config.yaml"
      provides: "ruff, ruff-format, mypy strict, and file-hygiene hooks"
      contains: "ruff-pre-commit"
  key_links:
    - from: ".github/workflows/*.yml"
      to: "pyproject.toml tool configs"
      via: "ruff/mypy/pytest read config from pyproject.toml installed via pixi"
      pattern: "pixi run|ruff|mypy|pytest"
    - from: ".pre-commit-config.yaml mirrors-mypy"
      to: "pydantic + types-readchar"
      via: "additional_dependencies so mypy resolves types in the hook env"
      pattern: "additional_dependencies"
---

<objective>
Add the CI gate (three tiered jobs — lint, typecheck, test — on a macOS × Python 3.11/3.12 matrix)
and the pre-commit configuration that enforces ruff, ruff-format, mypy strict, and file hygiene
before any commit reaches the remote.

Purpose: Guards every future commit. A push to a feature branch must trigger the CI matrix and a
ruff/type violation must be caught locally by pre-commit (SCAF-03, SCAF-04).
Output: `.github/workflows/lint.yml`, `.github/workflows/test.yml`, `.pre-commit-config.yaml`.
</objective>

<execution_context>
@/Users/ortizeg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ortizeg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Depends on 01-01: consumes the pyproject.toml tool configs (ruff/mypy/pytest), the pixi
# environment + tasks (notes/ruff/mypy/pytest), and the importable notes_os package + tests.
@.planning/phases/01-scaffold/01-01-SUMMARY.md

Locked CI / pre-commit specs (implement EXACTLY):

CI matrix: Python 3.11 AND 3.12 × macOS-latest ONLY. Ubuntu runners would fail (AppleScript is
macOS-exclusive). Default CI test command: `pytest -m 'not integration'`.

lint.yml — TWO separate jobs:
- `lint` job: `ruff check` + `ruff format --check`
- `typecheck` job: `mypy` (strict, from pyproject)
- both on the macos-latest × {3.11, 3.12} matrix

test.yml:
- `test` job: `pytest -m 'not integration' --cov --cov-fail-under=80` + codecov upload
- macos-latest × {3.11, 3.12} matrix

.pre-commit-config.yaml hooks:
- pre-commit-hooks: trailing-whitespace, end-of-file-fixer, check-yaml, check-toml,
  check-added-large-files (--maxkb=5000), check-merge-conflict, debug-statements
- ruff-pre-commit: ruff (with --fix) and ruff-format
- mirrors-mypy: mypy with additional_dependencies = ["pydantic>=2.0", "types-readchar"]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the CI workflows (lint + typecheck + test)</name>
  <files>.github/workflows/lint.yml, .github/workflows/test.yml</files>
  <action>
Create `.github/workflows/lint.yml`:
- name: "Lint & Typecheck". Triggers: on push (all branches) and pull_request.
- Two jobs, each with `runs-on: macos-latest` and `strategy.matrix.python-version: ["3.11", "3.12"]`,
  and `fail-fast: false`:
  - `lint`: checkout, set up Python (actions/setup-python@v5 with matrix python-version), install
    ruff (pin a recent version, e.g. `pip install ruff`), run `ruff check src tests` then
    `ruff format --check src tests`.
  - `typecheck`: checkout, set up Python, install the project + dev deps so mypy can resolve
    imports (`pip install -e .` plus `mypy pydantic types-readchar`), run `mypy src`.
- Prefer the github-actions skill conventions. Cache pip where reasonable. Do NOT use pixi inside
  CI unless trivial — a plain pip-based setup is simpler and avoids pixi-on-CI overhead; document
  the choice in the SUMMARY. (If you prefer pixi, use prefix-dev/setup-pixi and `pixi run ruff` /
  `pixi run mypy` — either is acceptable as long as both jobs run on the macOS × 3.11/3.12 matrix.)

Create `.github/workflows/test.yml`:
- name: "Test". Triggers: on push (all branches) and pull_request.
- One `test` job, `runs-on: macos-latest`, `strategy.matrix.python-version: ["3.11", "3.12"]`,
  `fail-fast: false`.
- Steps: checkout, set up Python, install the project + test deps (`pip install -e .` plus
  `pytest pytest-cov`), run `pytest -m 'not integration' --cov=notes_os --cov-report=xml
  --cov-fail-under=80`, then upload coverage via `codecov/codecov-action@v4` (token via
  `secrets.CODECOV_TOKEN`, but mark the upload step `continue-on-error: true` or guard it so a
  missing token does not fail the build before the repo is configured).

Both workflows must be valid YAML and runnable on the 01-01 scaffold (which already passes ruff,
mypy, and pytest locally).
  </action>
  <verify>`python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/lint.yml').read_text()); yaml.safe_load(pathlib.Path('.github/workflows/test.yml').read_text())"` succeeds; grep confirms `macos-latest`, `"3.11"`, `"3.12"`, `cov-fail-under=80`, and `not integration` appear.</verify>
  <done>Two workflow files exist and parse as valid YAML; the matrix is macOS-latest × 3.11/3.12; three tiered jobs (lint, typecheck, test) are defined; test job enforces the 80% coverage gate and excludes integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author .pre-commit-config.yaml and verify hooks reject violations</name>
  <files>.pre-commit-config.yaml</files>
  <action>
Create `.pre-commit-config.yaml` implementing the locked hook spec in <context> EXACTLY:
- `repos:` entry for `pre-commit/pre-commit-hooks` (pin a recent rev) with hooks:
  trailing-whitespace, end-of-file-fixer, check-yaml, check-toml,
  check-added-large-files (args: ["--maxkb=5000"]), check-merge-conflict, debug-statements.
- `astral-sh/ruff-pre-commit` (pin a recent rev) with hooks: `ruff` (args: ["--fix"]) and
  `ruff-format`.
- `pre-commit/mirrors-mypy` (pin a recent rev) with the `mypy` hook,
  `additional_dependencies: ["pydantic>=2.0", "types-readchar"]`, and
  `args: ["--strict"]` (or rely on pyproject config — but pass `files: ^src/` so it scans the
  package). Prefer the pre-commit skill conventions for rev pinning.

After writing, install and exercise the hooks to PROVE SCAF-04:
1. `pixi run pre-commit install` (or `pip install pre-commit && pre-commit install`).
2. `pre-commit run --all-files` — must pass clean on the 01-01 scaffold.
3. Prove rejection: temporarily create a throwaway file `src/notes_os/_tmp_violation.py` with a
   ruff violation (e.g. `import os` unused + a `print("x")` which T20 forbids) AND a function with
   a missing return annotation, run `pre-commit run --files src/notes_os/_tmp_violation.py`,
   CONFIRM it fails (ruff and/or mypy reject it), then DELETE the throwaway file so it is never
   committed. Record the observed failure in the SUMMARY.
  </action>
  <verify>`pre-commit run --all-files` exits 0 on the scaffold; the deliberate violation file is rejected by the hooks (non-zero exit) and is then removed (`test ! -e src/notes_os/_tmp_violation.py`).</verify>
  <done>.pre-commit-config.yaml exists with all specified hooks; hooks pass clean on the scaffold; a ruff/missing-annotation violation is demonstrably rejected before commit; no stray throwaway files remain.</done>
</task>

</tasks>

<verification>
- CI workflows parse as valid YAML, define lint + typecheck + test jobs on macOS-latest × 3.11/3.12 (SCAF-03).
- Test job runs `pytest -m 'not integration'` with `--cov-fail-under=80` and uploads to codecov.
- Pre-commit config installs and passes clean on the scaffold; a ruff violation / missing annotation is rejected locally (SCAF-04).
</verification>

<success_criteria>
- SCAF-03 satisfied: three-job tiered CI on the macOS × Python 3.11/3.12 matrix, green on the scaffold.
- SCAF-04 satisfied: pre-commit enforces ruff, ruff-format, mypy strict, and file hygiene; violations blocked before remote.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-02-SUMMARY.md`.
</output>
