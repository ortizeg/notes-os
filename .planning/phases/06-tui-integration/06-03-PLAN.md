---
phase: 06-tui-integration
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/notes_os/screens/task_extract.py
  - src/notes_os/screens/sort.py
  - src/notes_os/app.py
  - src/notes_os/app.tcss
  - tests/screens/test_task_extract_screen.py
autonomous: true
requirements: [TUI-04, TUI-05]
must_haves:
  truths:
    - "When config.features.task_extraction is True, TaskExtractScreen appears after routing (moving) each note that yields extracted tasks"
    - "When task_extraction is False (M1 default), TaskExtractScreen NEVER appears — the sort flow is byte-identical to 06-02"
    - "TaskExtractScreen lists the extracted tasks and supports Add-all / Select-subset / Skip, mirroring the SortUIProtocol.prompt_task_selection contract"
    - "Selected tasks are written via TaskWriter to the daily Markdown file (config.features.extracted_tasks_dir/YYYY-MM-DD.md) as '- [ ] {text}' lines"
    - "After the user resolves task selection, control returns to SortScreen and the sort flow advances to the next note"
    - "Esc/Skip dismisses without writing; ? shows the selection key legend — TUI-05 consistent"
  artifacts:
    - path: "src/notes_os/screens/task_extract.py"
      provides: "TaskExtractScreen reviewing extracted tasks after each move"
      contains: "class TaskExtractScreen"
    - path: "tests/screens/test_task_extract_screen.py"
      provides: "Pilot test proving SC3 (extraction enabled → screen appears → selection writes daily file) AND off-by-default proof"
  key_links:
    - from: "src/notes_os/screens/sort.py"
      to: "src/notes_os/screens/task_extract.py"
      via: "_after_move gated on config.features.task_extraction → push_screen TaskExtractScreen"
      pattern: "task_extraction"
    - from: "src/notes_os/screens/task_extract.py"
      to: "notes_os.sorter.extractor.extract_tasks / TaskWriter"
      via: "extract_tasks(note.preview) + TaskWriter(dir).write(selected)"
      pattern: "extract_tasks|TaskWriter|\\.write\\("
---

<objective>
Build TaskExtractScreen — the post-move task review step — and wire it into SortScreen's `_after_move` seam, gated on `config.features.task_extraction`.

After SortScreen successfully moves a note, when extraction is enabled the screen runs `extract_tasks(note.preview)`; if any tasks are found, TaskExtractScreen is shown so the user can Add-all / Select a subset / Skip. Selected tasks are persisted via `TaskWriter` to the daily Markdown checkbox file. This mirrors the EXISTING Phase-5 contract (`SortUIProtocol.prompt_task_selection` + `SortController._maybe_extract_tasks`) — reuse `extract_tasks` and `TaskWriter`; do NOT re-implement extraction or writing logic. The SC1 off-by-default gate from Phase 5 is preserved: when `task_extraction` is False (the M1 default), TaskExtractScreen never appears and the sort flow is byte-identical to 06-02.

Purpose: TUI-04 (TaskExtractScreen reviews extracted tasks after each note when extraction is enabled) and TUI-05 nav on the extract screen. Makes SC3 provable.

Output: `src/notes_os/screens/task_extract.py`, the SortScreen `_after_move` wiring, app.py SCREENS registry entry, app.tcss additions, and a Pilot test proving both the enabled-write path and the off-by-default path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Reuse these EXACTLY — pure extractor + daily-Markdown writer (do not re-implement):
@src/notes_os/sorter/extractor.py
# The Phase-5 selection contract to mirror (prompt_task_selection: add-all/select/skip) and the gate logic to copy:
@src/notes_os/sorter/controller.py
# The selection-prompt UI semantics already defined on the protocol:
@src/notes_os/sorter/ui.py
# Config: features.task_extraction (default False) + extracted_tasks_dir:
@src/notes_os/config.py
# SortScreen's _after_move seam + session/advance flow from 06-02:
@.planning/phases/06-tui-integration/06-02-SUMMARY.md
@src/notes_os/screens/sort.py
# Fixtures (tmp_path config, mock repo):
@tests/conftest.py
@tests/sorter/conftest.py
</context>

<environment_notes>
- `pixi` shell function is BROKEN. Use `.pixi/envs/default/bin/{ruff,mypy,pytest}`:
  - `.pixi/envs/default/bin/ruff format src tests` (EVERY new file before commit)
  - `.pixi/envs/default/bin/mypy src`
  - `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80`
- pre-commit NOT installed → run ruff format + ruff check + mypy MANUALLY before EACH commit.
- Discard pixi.lock churn: `git checkout -- pixi.lock`.
- pytest-asyncio `asyncio_mode=auto` configured by 06-01.
- TaskExtractScreen must NOT redefine extraction signals or the Markdown format — call `extract_tasks` and `TaskWriter.write` directly.
- app.py, sort.py, app.tcss are SHARED with 06-02 (earlier wave) and 06-04 (later wave). This is wave 3; edits are surgical (fill the `_after_move` seam, add one SCREENS entry).
- Never `print()` in `src/`; module docstring FIRST, then `from __future__ import annotations`.
- TaskExtractScreen defines NO Pydantic model → no pyproject mypy-override entry needed.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: TaskExtractScreen — list tasks, add-all / select / skip, write via TaskWriter</name>
  <files>src/notes_os/screens/task_extract.py, src/notes_os/app.tcss</files>
  <action>
Create `src/notes_os/screens/task_extract.py` with `class TaskExtractScreen(Screen[None])` (or a `ModalScreen[None]` if you want it to overlay the sort screen — modal is a good fit since it interrupts mid-flow; pick one and be consistent).

Constructor / data: the screen receives the list of `ExtractedTask` (already produced by `extract_tasks` in SortScreen — pass them in via `__init__`, do NOT re-extract here) and a `TaskWriter` (built from `self.app.app_config.features.extracted_tasks_dir`, or passed in). Store both.

compose(): render the PRD selection format — a header "Potential tasks found:", a numbered list of the task `.text` values (use a keyboard-navigable widget such as `SelectionList`/`OptionList`, or a numbered `Static` list plus number keys), and a footer legend `[A] Add all   [S] Select   [X] Skip`. Mirror `SortUIProtocol.prompt_task_selection` semantics (read ui.py):
  - **A** (Add all): select every task.
  - **S** (Select): enter a per-item toggle mode (Space toggles the highlighted item; Enter confirms the chosen subset). If using `SelectionList`, A pre-checks all and Enter confirms; keep the keys consistent with the documented contract.
  - **X** (Skip): select nothing.

On resolution, compute `selected: list[ExtractedTask]`. If non-empty, call `TaskWriter.write(selected)` (writes/append `- [ ] {text}` to `{extracted_tasks_dir}/YYYY-MM-DD.md`). Empty selection writes nothing (TaskWriter.write([]) is already a documented no-op). Then dismiss the screen and hand control back to SortScreen so the sort flow advances — use `self.dismiss(...)` (for ModalScreen) or `self.app.pop_screen()` and a callback/message so SortScreen knows to advance to the next note. Return the count or the selected list so SortScreen/tests can assert.

TUI-05 nav: Esc = same as Skip (dismiss without writing); ? = show the selection key legend overlay; ↑↓ move highlight; Enter confirm (in Select mode) / activate. Q quits the app (global). Keep these consistent with the other screens' meanings.

Add minimal styling to `src/notes_os/app.tcss` for the task list / modal (`#task-list`, modal centering). Surgical additions only.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src/notes_os/screens/task_extract.py src/notes_os/app.tcss && .pixi/envs/default/bin/mypy src && .pixi/envs/default/bin/python -c "import notes_os.screens.task_extract"</automated>
  </verify>
  <done>TaskExtractScreen lists ExtractedTasks, supports Add-all / Select-subset / Skip mirroring prompt_task_selection, writes the selected tasks via TaskWriter to the daily Markdown file, dismisses back to SortScreen, and exposes the result for assertion; defines no Pydantic model; mypy strict + ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SortScreen _after_move (gated) + register screen + SC3 Pilot test (enabled + off-by-default)</name>
  <files>src/notes_os/screens/sort.py, src/notes_os/app.py, tests/screens/test_task_extract_screen.py</files>
  <action>
Fill the `_after_move(note)` seam in `src/notes_os/screens/sort.py` (left as a no-op by 06-02). Copy the gate logic from `SortController._maybe_extract_tasks` EXACTLY in spirit:
  - FIRST line: `if not self.app.app_config.features.task_extraction: return` — the off-by-default short-circuit (preserves Phase-5 SC1; no extractor/writer/screen access on the disabled path).
  - Then `tasks = extract_tasks(note.preview)` (import `from notes_os.sorter.extractor import extract_tasks`). If `not tasks: return` (advance proceeds normally).
  - If tasks found: push/await the `TaskExtractScreen(tasks, writer)` (build `TaskWriter(self.app.app_config.features.extracted_tasks_dir)` — import lazily). Because Textual is async/event-driven, the "show screen, then advance after it resolves" must NOT block: use a screen-dismiss callback (`self.app.push_screen(TaskExtractScreen(...), self._on_tasks_resolved)`) where `_on_tasks_resolved` advances the sort flow to the next note. Restructure 06-02's advance logic so that, on the extraction-enabled path, advancing to the next note happens in the dismiss callback rather than synchronously after the move (so the user reviews tasks BEFORE the next note appears). On the disabled / no-tasks path, advance immediately as before. Keep the disabled path byte-identical to 06-02 (this is the important invariant).

Register the screen: add `"task_extract": TaskExtractScreen` to the `SCREENS` registry in `src/notes_os/app.py` (or push the instance directly with `push_screen(TaskExtractScreen(...))` — if you pass constructor args, instance-push is required; registry entry optional). Import lazily to keep `import notes_os.app` cheap. Surgical edit only.

Write `tests/screens/test_task_extract_screen.py` proving SC3 with two cases, both via Pilot, no AppleScript:
  1. **Enabled write path:** build a `tui_config` variant with `features.task_extraction=True` and `features.extracted_tasks_dir` under `tmp_path`. Seed the mock repo with a note whose `.preview` contains a known signal phrase (e.g. `"I need to follow up with Sam by Monday."` — matches the action + by-weekday families in extractor.py) and that routes via a single keystroke (Archive `X` or Areas `A`). Drive Home→Sort, route the note, then assert the TaskExtractScreen appeared (`isinstance(app.screen, TaskExtractScreen)` after the move keystroke + `pilot.pause()`). Press `a` (Add all) then confirm, and assert the daily Markdown file `{extracted_tasks_dir}/<today>.md` exists and contains a `- [ ] ` line with the extracted text. (Inject a fixed-date clock into TaskWriter if needed for a deterministic filename, OR just glob the one created file.)
  2. **Off-by-default path:** identical seed but `features.task_extraction=False` (the default). Route the same note and assert the TaskExtractScreen NEVER appears (`app.screen` is still the SortScreen, or the flow advanced straight to the next note / finish), and assert NO file was written under `extracted_tasks_dir` (directory absent or empty). This proves the M1 default keeps extraction fully inert.
  Optionally add a Skip-path case: enabled, press `x` (Skip) on the extract screen, assert no file written and the flow advanced.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src/notes_os/screens/sort.py src/notes_os/app.py tests/screens/test_task_extract_screen.py && .pixi/envs/default/bin/mypy src && .pixi/envs/default/bin/pytest tests/screens/test_task_extract_screen.py -p no:cacheprovider -q</automated>
  </verify>
  <done>SortScreen._after_move is gated on config.features.task_extraction (off-by-default short-circuit byte-identical to 06-02); when enabled and tasks are found, TaskExtractScreen appears and Add-all writes the daily Markdown file; the off-by-default Pilot case proves no screen and no file when the flag is False; mypy strict + ruff clean; overall coverage ≥80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| note.preview → extract_tasks | untrusted note text scanned by pure ReDoS-safe regexes (Phase-5 extractor) |
| TaskExtractScreen → filesystem | TaskWriter writes only under config.extracted_tasks_dir (tmp_path in tests) |
| feature flag → screen activation | task_extraction gate decides whether the screen ever runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | extraction running when disabled | mitigate | `_after_move` first line returns when task_extraction is False; off-by-default Pilot test asserts no screen + no file |
| T-06-09 | Information disclosure | writing tasks outside the configured dir | mitigate | TaskWriter writes only under extracted_tasks_dir (reused Phase-5 class); tests point it at tmp_path |
| T-06-10 | Denial of service | extraction blocking the event loop | mitigate | extract_tasks is pure/fast; the screen is shown via push_screen with a dismiss callback (non-blocking advance) |
</threat_model>

<verification>
- `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80` green.
- `.pixi/envs/default/bin/mypy src` zero errors; ruff clean.
- Disabled-path invariant: with `task_extraction=False`, the SortScreen flow is byte-identical to 06-02 (no TaskExtractScreen import-time cost on that branch; no file writes) — asserted by the off-by-default test.
- extractor.py and TaskWriter are UNMODIFIED (reused, not re-implemented): `git diff --stat src/notes_os/sorter/extractor.py` shows no change.
</verification>

<success_criteria>
- TUI-04: TaskExtractScreen reviews extracted tasks after each move when extraction is enabled.
- SC3 provable: enabled → screen appears → Add-all writes the daily Markdown file; off-by-default → no screen, no file — both via Pilot with tmp_path config.
- TUI-05: Esc=Skip, ? legend, ↑↓/Enter selection — consistent with Home/Sort screens.
- Phase-5 SC1 off-by-default behavior preserved (gate is the first line of `_after_move`).
</success_criteria>

<output>
Create `.planning/phases/06-tui-integration/06-03-SUMMARY.md` when done.
</output>
