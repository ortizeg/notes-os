---
phase: 06-tui-integration
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - src/notes_os/app.py
  - src/notes_os/screens/home.py
  - src/notes_os/screens/sort.py
  - src/notes_os/screens/task_extract.py
  - src/notes_os/app.tcss
  - tests/screens/test_navigation.py
  - tests/screens/test_end_to_end.py
  - tests/sorter/test_notes_integration.py
  - .planning/phases/06-tui-integration/06-04-SUMMARY.md
autonomous: true
requirements: [TUI-01, TUI-02, TUI-03, TUI-04, TUI-05]
must_haves:
  truths:
    - "Navigation is consistent across all three screens: ↑↓ move, Enter select/confirm, Esc/B back one level, Q quits from any screen (with confirm if a sort session is in progress), ? shows contextual help"
    - "Q from any screen quits the app; when a sort session has recorded moves/skips, a confirm prompt guards the quit"
    - "? on each screen shows that screen's contextual help legend"
    - "A single end-to-end Pilot test walks Home → Sort → (extraction) → finish with a backup-wrapped MockNotesRepository, proving SC2 + SC3 in one flow"
    - "The full Pilot suite passes under plain pytest and the overall coverage gate stays ≥80%"
    - "An optional @pytest.mark.integration smoke test launches the app against real Notes but is deselected in CI"
  artifacts:
    - path: "tests/screens/test_navigation.py"
      provides: "SC4 nav-convention Pilot tests across all screens (Esc/B/Q/?)"
    - path: "tests/screens/test_end_to_end.py"
      provides: "Single Home→Sort→extract→finish end-to-end Pilot walk"
    - path: "tests/sorter/test_notes_integration.py"
      provides: "Optional @pytest.mark.integration TUI smoke test (deselected in CI)"
  key_links:
    - from: "src/notes_os/app.py"
      to: "all three screens"
      via: "shared BINDINGS convention + quit-confirm action"
      pattern: "BINDINGS|action_quit"
    - from: "tests/screens/test_end_to_end.py"
      to: "BackingUpNotesRepository over MockNotesRepository"
      via: "injected repo proving backup-then-move across the full walk"
      pattern: "BackingUpNotesRepository"
---

<objective>
Finalize Phase 6: make the navigation conventions consistent and complete across all three screens, add the quit-confirm guard for in-progress sessions and the contextual help overlays, write the comprehensive Textual Pilot test suite that proves SC1–SC4 end-to-end, add the optional integration smoke test, and confirm the overall ≥80% coverage gate is green.

This plan is the integration/wiring/verification pass. It harmonizes the per-screen bindings introduced in 06-01/02/03 into ONE consistent convention (TUI-05), adds the cross-cutting behaviors that span screens (quit-confirm when a session is in progress, contextual ? help), and writes the suite that exercises the whole product as one cohesive flow — the last step of Milestone 1.

Purpose: TUI-05 (consistent nav across screens) plus end-to-end verification of TUI-01..04. Makes SC4 and SC5 provable.

Output: harmonized bindings + quit-confirm + help overlays across app.py and the three screens, `tests/screens/test_navigation.py`, `tests/screens/test_end_to_end.py`, an optional integration smoke test in `tests/sorter/test_notes_integration.py`, and a green ≥80% coverage run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# The three screens + app shell to harmonize (read the SUMMARYs for the seams each left):
@.planning/phases/06-tui-integration/06-01-SUMMARY.md
@.planning/phases/06-tui-integration/06-02-SUMMARY.md
@.planning/phases/06-tui-integration/06-03-SUMMARY.md
@src/notes_os/app.py
@src/notes_os/screens/home.py
@src/notes_os/screens/sort.py
@src/notes_os/screens/task_extract.py
# Session count drives the quit-confirm guard:
@src/notes_os/sorter/session.py
# Fixtures:
@tests/conftest.py
@tests/sorter/conftest.py
# Existing integration-test file to extend (pattern for @pytest.mark.integration + _TestInbox):
@tests/sorter/test_notes_integration.py
</context>

<environment_notes>
- `pixi` shell function is BROKEN. Use `.pixi/envs/default/bin/{ruff,mypy,pytest}`:
  - `.pixi/envs/default/bin/ruff format src tests` (EVERY new file before commit)
  - `.pixi/envs/default/bin/ruff check src tests`
  - `.pixi/envs/default/bin/mypy src`
  - `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80` (the CI command — must end green)
- pre-commit NOT installed → run ruff format + ruff check + mypy MANUALLY before EACH commit.
- Discard pixi.lock churn: `git checkout -- pixi.lock`.
- This is wave 4 — it serializes AFTER 06-01/02/03 because it edits the SHARED files (app.py, home.py, sort.py, task_extract.py, app.tcss) they created. There is no file-overlap conflict because no earlier wave runs concurrently with this one.
- Screens are terminal-coupled so per-screen line coverage may be modest; the OVERALL ≥80% gate is what must stay green (core modules are ~100%). Do NOT chase 100% on screen render branches.
- Never `print()` in `src/`; module docstring FIRST, then `from __future__ import annotations`.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize TUI-05 nav — shared bindings, quit-confirm guard, contextual help overlays</name>
  <files>src/notes_os/app.py, src/notes_os/screens/home.py, src/notes_os/screens/sort.py, src/notes_os/screens/task_extract.py, src/notes_os/app.tcss</files>
  <action>
Audit the bindings that 06-01/02/03 declared on each screen and reconcile them into ONE documented convention (TUI-05). The single source of truth for the convention: ↑/↓ move selection, Enter select/confirm, Esc (and B in menu contexts) back one level, Q quit, ? help. Implement the cross-cutting pieces that span screens:

1. **Quit-confirm guard (Q with session in progress).** A bare Q quits immediately from Home/idle. But when a sort session is in progress (the SortScreen's `SortSession` has recorded any moves/skips/errors, i.e. `summary().total > 0` and not yet finished), Q must show a confirm prompt before quitting (TUI-05 says "Q quits from any screen (with confirm if a sort session is in progress)"). Implement via an app-level `action_quit` override that checks a flag/state on the active SortScreen (e.g. an `app.sort_in_progress` boolean the SortScreen sets True on first record and False at `_finish()`), and pushes a small confirm `ModalScreen` (Yes quits via `self.exit()`, No dismisses). On Home/TaskExtract (no active session) Q quits without the prompt. Keep this minimal — a two-button/`y`-`n` modal is fine.

2. **Contextual ? help overlay per screen.** Ensure each screen's `?` shows a help legend specific to THAT screen (Home: menu + global keys; Sort: PARA key legend + ↑↓/number/Esc/B; TaskExtract: A/S/X + Esc). If 06-01/02/03 implemented these as `notify` or inline overlays, standardize them to one mechanism (a shared lightweight help modal or consistent `notify` legends). Factor the common legend text / help-modal into a small shared helper (e.g. a function or a `HelpModal(ModalScreen)` taking a legend string) so the three screens render help consistently. Avoid duplicating three divergent help implementations.

3. **Esc/B back-one-level consistency.** Verify Esc/B semantics are uniform: SortScreen subfolder→folder→category→Home; TaskExtract Esc=Skip+dismiss; Home Esc is a no-op or routes to quit-confirm. Fix any divergence so the same keys mean the same kind of action everywhere.

Update `app.tcss` for the confirm/help modals (centering, dim background). Keep edits surgical — do not regress 06-01/02/03 behavior; re-run their tests after editing (Task 2 covers the suite). Run `.pixi/envs/default/bin/mypy src` and `ruff format`/`check` after.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format src tests && .pixi/envs/default/bin/ruff check src tests && .pixi/envs/default/bin/mypy src && .pixi/envs/default/bin/pytest tests/screens -p no:cacheprovider -q</automated>
  </verify>
  <done>All three screens share one nav convention; Q shows a confirm modal when a sort session is in progress and quits directly otherwise; each screen's ? shows a contextual help legend via a shared helper; Esc/B back-one-level is uniform; the prior 06-01/02/03 screen tests still pass; mypy strict + ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive Pilot suite — SC4 navigation + SC1–SC3 end-to-end walk + coverage gate</name>
  <files>tests/screens/test_navigation.py, tests/screens/test_end_to_end.py</files>
  <action>
Write `tests/screens/test_navigation.py` proving SC4 across all screens (Pilot, no AppleScript). Cases:
  - **Esc/B back:** on SortScreen, drill `p` → AWAIT_FOLDER, press `escape` → back at category; press `b` from category (with no session moves) → returns to HomeScreen. Assert the active screen / router state after each.
  - **Q quit (no session):** from HomeScreen press `q` → app exits (the `run_test()` context ends / `app.return_value`/`app._exit` set). Assert the app stopped without a confirm modal.
  - **Q quit-confirm (session in progress):** start a sort, route nothing-yet but force a session record (route one note so `summary().total > 0`), press `q` → assert a confirm modal appears (`isinstance(app.screen, <Help/Confirm modal>)` or query the modal), then press `n` → still running on SortScreen; press `q` then `y` → app exits. (If the in-progress flag is set on first record, route one note first.)
  - **? help per screen:** on each of Home / Sort / TaskExtract, press `?` and assert the contextual help overlay/legend is shown (query the help modal or assert the notify/legend text contains screen-specific keys, e.g. Sort help contains "Projects"/"Areas", TaskExtract help contains "Add all"/"Skip").

Write `tests/screens/test_end_to_end.py` — one cohesive walk proving SC2 + SC3 together (Pilot, no AppleScript):
  - `tui_config` with `features.task_extraction=True`, all dirs under tmp_path; a stub `NoteStore.sqlite` under the tmp notes_db_dir so backups can be created.
  - Inject `BackingUpNotesRepository(MockNotesRepository(seed), BackupManager(backup_config), backup_config)` so backup-then-move is real. Seed 2 notes: one with a task-signal preview routed via a single key, one plain.
  - Walk: launch → HomeScreen shows inbox count 2 (SC1 spot-check) → activate "Sort Inbox" → SortScreen → route note 1 (single keystroke) → backup dir now exists + move recorded (SC2) → TaskExtractScreen appears for note 1's tasks → Add-all → daily Markdown file written (SC3) → flow advances to note 2 → route note 2 → finish → SortScreen shows the summary (moved==2) and the audit log file exists under `config.log_dir`.
  - Assert across the walk: `mock.moves` length 2, inbox empty, a backup directory exists, the daily `.md` file has the extracted `- [ ] ` line, the session log file exists. This single test is the M1 "whole product works" proof.

Finally run the FULL CI command and confirm the overall coverage gate is green:
  `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80`
If the gate dips below 80% (screens dragging it down), add targeted assertions/paths in the screen tests to cover the main render/branch paths — do NOT lower the threshold and do NOT chase 100% on cosmetic render branches.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format tests/screens/test_navigation.py tests/screens/test_end_to_end.py && .pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80 -p no:cacheprovider -q</automated>
  </verify>
  <done>test_navigation.py proves SC4 (Esc/B back, Q quit + quit-confirm, ? contextual help) across all screens; test_end_to_end.py walks Home→Sort→extract→finish proving SC1+SC2+SC3 in one flow with a backup-wrapped MockNotesRepository; the full `pytest -m 'not integration' --cov-fail-under=80` run is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Optional integration smoke test (deselected in CI)</name>
  <files>tests/sorter/test_notes_integration.py</files>
  <action>
Add ONE `@pytest.mark.integration` test to `tests/sorter/test_notes_integration.py` (extend the existing file; follow its existing `_TestInbox` / macOS-only pattern — read the file first) that launches the REAL `NotesOSApp()` (no injected repo, so it builds the production AppleScript + backup repo) against the test inbox and drives a minimal Pilot interaction (e.g. open Home, read the live inbox count, press `q` to quit) — proving the production wiring path runs against real Notes. This MUST be marked `@pytest.mark.integration` so the default CI run (`addopts = -m 'not integration'`) deselects it — it never runs in CI and never touches real user notes (uses `_TestInbox`). If launching a full Textual app against a real terminal is impractical headlessly, keep the smoke test minimal: instantiate `NotesOSApp()` and assert it builds the production `BackingUpNotesRepository` over `AppleScriptNotesRepository` (verify the repo types), guarded by the integration marker. Document in the test docstring that it is macOS-only and deselected in CI.

Verify it is deselected by the default run: `.pixi/envs/default/bin/pytest -m 'not integration' -q` collects but does NOT execute it, and `-m integration --collect-only` shows it exists.
  </action>
  <verify>
    <automated>.pixi/envs/default/bin/ruff format tests/sorter/test_notes_integration.py && .pixi/envs/default/bin/ruff check tests && .pixi/envs/default/bin/pytest -m integration --collect-only -q tests/sorter/test_notes_integration.py && .pixi/envs/default/bin/pytest -m 'not integration' -q tests/sorter/test_notes_integration.py</automated>
  </verify>
  <done>An @pytest.mark.integration TUI smoke test exists, is collected under `-m integration`, and is deselected (not executed) by the default `-m 'not integration'` CI run; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard → any screen | quit/help/back keys act uniformly; Q during a session is guarded by a confirm |
| app → real Notes (integration only) | the production path is exercised only by the integration-marked test against _TestInbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Denial of service | accidental Q mid-session losing in-flight triage state | mitigate | Q during an in-progress session shows a confirm modal before exiting; nav test asserts the guard |
| T-06-12 | Tampering | integration test mutating real user notes | mitigate | integration test uses _TestInbox and is deselected in CI; never runs against real user data automatically |
| T-06-13 | Repudiation | session outcomes not durably recorded | mitigate | end-to-end test asserts the audit log file is written under config.log_dir at finish |
</threat_model>

<verification>
- `.pixi/envs/default/bin/pytest -m 'not integration' --cov=notes_os --cov-fail-under=80` → GREEN (SC5; the CI command).
- `.pixi/envs/default/bin/mypy src` zero errors; `ruff check src tests` + `ruff format --check src tests` clean.
- Integration test is collected under `-m integration` and NOT executed under `-m 'not integration'`.
- readchar CLI path still imports and is unchanged.
- `import notes_os.app` does not eagerly import AppleScript (deferred imports preserved).
</verification>

<success_criteria>
- TUI-05: navigation consistent across Home/Sort/TaskExtract — ↑↓ move, Enter select, Esc/B back, Q quit (confirm if session in progress), ? contextual help.
- SC4 provable via test_navigation.py.
- SC1+SC2+SC3 provable in one cohesive walk via test_end_to_end.py with a backup-wrapped MockNotesRepository (no AppleScript).
- SC5: overall ≥80% coverage gate green on the default CI run; the optional integration smoke test is deselected in CI.
- Milestone 1 complete: the `notes` command launches a full Textual app with Home, Sort, and TaskExtract screens working end-to-end.
</success_criteria>

<output>
Create `.planning/phases/06-tui-integration/06-04-SUMMARY.md` when done.
</output>
