"""Application configuration models for NotesOS.

Pydantic V2 frozen config models, TOML loader, and defaults arrive in Phase 4.
This stub ensures ``import notes_os.config`` always succeeds so other modules
can import the namespace unconditionally from day one.
"""

from __future__ import annotations
